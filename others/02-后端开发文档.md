# 后端开发文档

## 概述

本文档定义了基于 FastAPI 的后端服务架构、API 设计、核心业务逻辑实现和部署配置。

---

## 一、项目结构

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                 # 应用入口
│   ├── core/                   # 核心配置
│   │   ├── __init__.py
│   │   ├── config.py           # 应用配置
│   │   ├── database.py         # 数据库连接（异步/同步）
│   │   ├── security.py         # 安全相关（JWT、密码哈希）
│   │   ├── permissions.py      # RBAC 权限码 + require_permission 依赖工厂
│   │   ├── celery_app.py       # Celery 配置
│   │   ├── dependencies.py     # 通用依赖注入
│   │   └── logging.py          # JSON 结构化日志
│   ├── api/                    # API 路由
│   │   ├── __init__.py
│   │   └── v1/
│   │       ├── __init__.py
│   │       ├── router.py       # 路由聚合
│   │       ├── auth.py         # 认证相关
│   │       ├── users.py        # 用户管理
│   │       ├── documents.py    # 文档管理
│   │       ├── tasks.py        # 任务管理
│   │       ├── annotations.py  # 标注相关
│   │       ├── operation_logs.py  # 操作日志
│   │       ├── statistics.py   # 工作量统计
│   │       ├── comments.py     # 评论讨论
│   │       ├── notifications.py  # 通知
│   │       ├── permissions.py  # 权限管理
│   │       ├── knowledge_graph.py  # 知识图谱
│   │       ├── active_learning.py  # 主动学习
│   │       ├── version_management.py  # 版本管理
│   │       ├── calibration_drift.py  # 校准与漂移检测
│   │       └── models.py       # 模型管理
│   ├── models/                 # SQLAlchemy 数据库模型
│   │   ├── __init__.py
│   │   ├── base.py             # 基础模型类
│   │   ├── user.py             # 用户模型
│   │   ├── document.py         # 文档模型
│   │   ├── task.py             # 任务模型
│   │   ├── annotation.py       # 标注模型
│   │   ├── comment.py          # 评论/通知模型
│   │   ├── role.py             # 角色模型
│   │   ├── system_config.py    # 系统配置模型
│   │   └── model.py            # 模型注册
│   ├── schemas/                # Pydantic 数据验证模型
│   │   ├── __init__.py
│   │   ├── base.py             # 基础 Schema
│   │   ├── user.py             # 用户 Schema
│   │   ├── document.py         # 文档 Schema
│   │   ├── task.py             # 任务 Schema
│   │   ├── annotation.py       # 标注 Schema
│   │   ├── operation_log.py    # 操作日志 Schema
│   │   ├── comment.py          # 评论/通知 Schema
│   │   ├── role.py             # 角色/权限 Schema
│   │   └── response.py         # 统一响应 Schema
│   ├── services/               # 业务逻辑层
│   │   ├── __init__.py
│   │   ├── auth_service.py     # 认证服务
│   │   ├── user_service.py     # 用户服务
│   │   ├── document_service.py # 文档服务
│   │   ├── parsing_service.py  # 解析服务
│   │   ├── extraction_service.py  # 抽取服务（规则 + NER + REBEL 融合）
│   │   ├── ner_service.py      # NER 抽取（SciBERT 微调，可插拔降级）
│   │   ├── rebel_service.py    # 关系抽取（REBEL 关系分类，可插拔降级）
│   │   ├── task_service.py     # 任务服务
│   │   ├── annotation_service.py  # 标注服务
│   │   ├── operation_log_service.py  # 操作日志服务
│   │   ├── comment_service.py  # 评论服务（@提及解析/通知）
│   │   ├── notification_service.py  # 通知服务
│   │   ├── permission_service.py  # 角色权限服务
│   │   ├── graph_service.py    # 图谱服务
│   │   ├── model_service.py    # 模型服务
│   │   ├── statistics_service.py  # 工作量统计服务
│   │   ├── reference_service.py   # 参考文献解析
│   │   ├── ocr_service.py         # OCR（PaddleOCR，可插拔降级）
│   │   ├── layout_service.py      # 版面分析（LayoutLMv3，可插拔降级）
│   │   ├── formula_service.py     # 公式识别（LaTeX-OCR，可插拔降级）
│   │   ├── figure_detection_service.py  # 图表/公式检测（YOLOv8）
│   │   ├── chart_description_service.py # 图表描述（BLIP-2）
│   │   ├── entity_linking_service.py    # 实体链接（Wikidata/DBpedia）
│   │   ├── relation_inference_service.py # 关系推理/图谱补全（TransE）
│   │   ├── semantic_search_service.py   # 语义搜索
│   │   ├── text_to_cypher_service.py    # Text-to-Cypher
│   │   ├── active_learning_service.py   # 主动学习采样
│   │   ├── annotation_agreement_service.py # 标注一致性评估
│   │   ├── version_management_service.py   # 数据/模型版本管理
│   │   ├── calibration_drift_service.py    # 置信度校准与漂移检测
│   │   └── graphrag_integration.py         # GraphRAG 集成
│   ├── workers/                # Celery 异步任务
│   │   ├── __init__.py
│   │   ├── parsing_tasks.py    # 解析任务
│   │   ├── extraction_tasks.py # 抽取任务
│   │   └── graph_tasks.py      # 图谱构建任务
│   ├── utils/                  # 工具函数
│   │   ├── __init__.py
│   │   ├── minio_client.py     # MinIO 客户端
│   │   ├── neo4j_client.py     # Neo4j 客户端
│   │   ├── redis_client.py     # Redis 客户端
│   │   ├── pagination.py       # 分页工具
│   │   └── metrics.py          # Prometheus 指标
│   └── middleware/             # 中间件
│       ├── __init__.py
│       └── logging.py          # 日志中间件（CORS 在 main.py 中配置）
├── alembic/                    # 数据库迁移
│   ├── versions/
│   └── env.py
├── storage/                    # 上传文件本地暂存
│   └── uploads/                # 暂存目录
├── scripts/                    # 脚本
│   └── init_db.py              # 数据库初始化
├── .env                        # 环境变量
├── .env.example                # 环境变量示例
├── alembic.ini                 # Alembic 迁移配置
├── Dockerfile
├── requirements.txt
├── requirements-dev.txt        # 开发依赖（Ruff/Black/pytest）
├── pyproject.toml
├── start.sh                    # 启动脚本
├── COMPLETION_SUMMARY.md
└── README_BACKEND.md
```

---

## 二、核心配置

### 2.1 应用配置（config.py）

```python
from pydantic_settings import BaseSettings, SettingsConfigDict
from functools import lru_cache

class Settings(BaseSettings):
    # 应用基础配置
    APP_NAME: str = "Scientific Document Parser"
    APP_VERSION: str = "1.0.0"
    DEBUG: bool = False
    API_PREFIX: str = "/api/v1"
    
    # 数据库配置
    DATABASE_URL: str = "mysql+aiomysql://root:20020522@localhost:3306/wx"
    DATABASE_POOL_SIZE: int = 20
    DATABASE_MAX_OVERFLOW: int = 10
    
    # Redis 配置
    REDIS_URL: str = "redis://localhost:6379/0"
    
    # Neo4j 配置
    NEO4J_URI: str = "bolt://localhost:7687"
    NEO4J_USER: str = "neo4j"
    NEO4J_PASSWORD: str = "password"
    
    # MinIO/S3 配置
    MINIO_ENDPOINT: str = "localhost:9000"
    MINIO_ACCESS_KEY: str = "minioadmin"
    MINIO_SECRET_KEY: str = "minioadmin"
    MINIO_BUCKET_NAME: str = "documents"
    MINIO_SECURE: bool = False
    
    # JWT 配置
    JWT_SECRET_KEY: str = "your-secret-key-change-in-production"
    JWT_ALGORITHM: str = "HS256"
    JWT_ACCESS_TOKEN_EXPIRE_MINUTES: int = 30
    JWT_REFRESH_TOKEN_EXPIRE_DAYS: int = 7
    
    # Celery 配置
    CELERY_BROKER_URL: str = "redis://localhost:6379/1"
    CELERY_RESULT_BACKEND: str = "redis://localhost:6379/2"
    
    # AI 模型配置
    EMBEDDING_MODEL_PATH: str = "moulds/bge-m3"
    LLM_MODEL_PATH: str = "moulds/qwen-7b"
    LLM_MAX_TOKENS: int = 4096
    LLM_TEMPERATURE: float = 0.7
    
    # 文件上传配置
    MAX_UPLOAD_SIZE: int = 100 * 1024 * 1024  # 100MB
    ALLOWED_EXTENSIONS: set = {"pdf"}
    
    # 日志配置
    LOG_LEVEL: str = "INFO"
    LOG_FORMAT: str = "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
    
    model_config = SettingsConfigDict(env_file=".env", case_sensitive=True)

@lru_cache()
def get_settings() -> Settings:
    return Settings()

settings = get_settings()
```

### 2.2 安全配置（security.py）

```python
from datetime import datetime, timedelta
from typing import Optional
from jose import JWTError, jwt
from passlib.context import CryptContext
from fastapi import Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer
from app.core.config import settings

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
oauth2_scheme = OAuth2PasswordBearer(tokenUrl=f"{settings.API_PREFIX}/auth/login")

def verify_password(plain_password: str, hashed_password: str) -> bool:
    return pwd_context.verify(plain_password, hashed_password)

def get_password_hash(password: str) -> str:
    return pwd_context.hash(password)

def create_access_token(data: dict, expires_delta: Optional[timedelta] = None) -> str:
    to_encode = data.copy()
    expire = datetime.utcnow() + (expires_delta or timedelta(minutes=settings.JWT_ACCESS_TOKEN_EXPIRE_MINUTES))
    to_encode.update({"exp": expire, "type": "access"})
    return jwt.encode(to_encode, settings.JWT_SECRET_KEY, algorithm=settings.JWT_ALGORITHM)

def create_refresh_token(data: dict) -> str:
    to_encode = data.copy()
    expire = datetime.utcnow() + timedelta(days=settings.JWT_REFRESH_TOKEN_EXPIRE_DAYS)
    to_encode.update({"exp": expire, "type": "refresh"})
    return jwt.encode(to_encode, settings.JWT_SECRET_KEY, algorithm=settings.JWT_ALGORITHM)

def decode_token(token: str) -> dict:
    try:
        payload = jwt.decode(token, settings.JWT_SECRET_KEY, algorithms=[settings.JWT_ALGORITHM])
        return payload
    except JWTError:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token")

async def get_current_user(token: str = Depends(oauth2_scheme)) -> dict:
    payload = decode_token(token)
    if payload.get("type") != "access":
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token type")
    user_id = payload.get("sub")
    if user_id is None:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token")
    return {"user_id": user_id, "role": payload.get("role")}

def require_role(required_role: str):
    async def role_checker(current_user: dict = Depends(get_current_user)):
        if current_user["role"] not in ["admin", required_role]:
            raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="Insufficient permissions")
        return current_user
    return role_checker
```

---

## 三、API 路由设计

### 3.1 路由聚合（router.py）

```python
from fastapi import APIRouter
from app.api.v1 import auth, users, documents, tasks, annotations, knowledge_graph, models, operation_logs, statistics, comments, notifications, permissions

router = APIRouter()

router.include_router(auth.router, prefix="/auth", tags=["认证"])
router.include_router(users.router, prefix="/users", tags=["用户管理"])
router.include_router(documents.router, prefix="/documents", tags=["文档管理"])
router.include_router(tasks.router, prefix="/tasks", tags=["任务管理"])
router.include_router(annotations.router, prefix="/annotations", tags=["标注管理"])
router.include_router(operation_logs.router, prefix="/operation-logs", tags=["操作日志"])
router.include_router(statistics.router, prefix="/statistics", tags=["工作量统计"])
router.include_router(comments.router, prefix="/comments", tags=["评论讨论"])
router.include_router(notifications.router, prefix="/notifications", tags=["通知"])
router.include_router(permissions.router, prefix="/permissions", tags=["权限管理"])
router.include_router(knowledge_graph.router, prefix="/knowledge-graph", tags=["知识图谱"])
router.include_router(models.router, prefix="/models", tags=["模型管理"])
```

### 3.2 认证 API（auth.py）

```python
from fastapi import APIRouter, Depends, HTTPException, status
from pydantic import BaseModel, EmailStr
from app.core.security import create_access_token, create_refresh_token, verify_password, get_current_user
from app.services.auth_service import authenticate_user, get_user_by_email

router = APIRouter()

class LoginRequest(BaseModel):
    username: str
    password: str

class TokenResponse(BaseModel):
    access_token: str
    refresh_token: str
    token_type: str = "bearer"

class RefreshRequest(BaseModel):
    refresh_token: str

@router.post("/login", response_model=TokenResponse)
async def login(request: LoginRequest):
    user = await authenticate_user(request.username, request.password)
    if not user:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Incorrect username or password")
    
    access_token = create_access_token(data={"sub": str(user.id), "role": user.role})
    refresh_token = create_refresh_token(data={"sub": str(user.id)})
    
    return TokenResponse(access_token=access_token, refresh_token=refresh_token)

@router.post("/refresh", response_model=TokenResponse)
async def refresh_token(request: RefreshRequest):
    # 验证 refresh token 并生成新的 token 对
    pass

@router.post("/logout")
async def logout(current_user: dict = Depends(get_current_user)):
    # 使 refresh token 失效
    pass

@router.post("/register")
async def register(
    username: str,
    email: EmailStr,
    password: str,
    full_name: str = None
):
    # 用户注册逻辑
    pass
```

### 3.3 文档管理 API（documents.py）

```python
from fastapi import APIRouter, Depends, UploadFile, File, Query, HTTPException, status
from typing import List, Optional
from app.core.permissions import DOCUMENT_READ, DOCUMENT_WRITE, has_permission, require_permission
from app.schemas.document import DocumentCreate, DocumentUpdate, DocumentResponse, DocumentListResponse
from app.services.document_service import DocumentService
from app.utils.pagination import PaginationParams

router = APIRouter()

@router.post("/upload", response_model=DocumentResponse, status_code=status.HTTP_201_CREATED)
async def upload_document(
    file: UploadFile = File(...),
    title: str = None,
    current_user: dict = Depends(require_permission(DOCUMENT_WRITE))
):
    """上传 PDF 文档"""
    if file.content_type != "application/pdf":
        raise HTTPException(status_code=400, detail="Only PDF files are allowed")
    
    service = DocumentService()
    return await service.upload_document(file, title, current_user["user_id"])

@router.get("/", response_model=DocumentListResponse)
async def list_documents(
    pagination: PaginationParams = Depends(),
    status: Optional[str] = None,
    keyword: Optional[str] = None,
    current_user: dict = Depends(require_permission(DOCUMENT_READ))
):
    """获取文档列表（支持分页和筛选）"""
    service = DocumentService()
    scope_all = has_permission(current_user, DOCUMENT_WRITE)
    return await service.list_documents(pagination, status, keyword, current_user["user_id"], scope_all)

@router.get("/{document_id}", response_model=DocumentResponse)
async def get_document(
    document_id: str,
    current_user: dict = Depends(require_permission(DOCUMENT_READ))
):
    """获取文档详情"""
    service = DocumentService()
    scope_all = has_permission(current_user, DOCUMENT_WRITE)
    return await service.get_document(document_id, current_user["user_id"], scope_all)

@router.delete("/{document_id}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_document(
    document_id: str,
    current_user: dict = Depends(require_permission(DOCUMENT_WRITE))
):
    """删除文档"""
    service = DocumentService()
    await service.delete_document(document_id, current_user["user_id"], True)

@router.post("/{document_id}/parse", response_model=dict)
async def parse_document(
    document_id: str,
    current_user: dict = Depends(require_permission(DOCUMENT_WRITE))
):
    """触发文档解析任务"""
    service = DocumentService()
    return await service.trigger_parsing(document_id, current_user["user_id"], True)

@router.get("/{document_id}/pages/{page_number}/elements")
async def get_page_elements(
    document_id: str,
    page_number: int,
    current_user: dict = Depends(require_permission(DOCUMENT_READ))
):
    """获取指定页面的元素列表"""
    pass
```

> 权限接线（RBAC）：上传/更新/删除/解析 → `document:write`；列表/详情/页面元素 → `document:read`。非全局范围仅能访问本人资源（数据级权限）。

### 3.4 任务管理 API（tasks.py）

```python
from fastapi import APIRouter, Depends, Query, HTTPException
from typing import Optional
from app.core.permissions import TASK_READ, TASK_WRITE, has_permission, require_permission
from app.schemas.task import TaskCreate, TaskAssign, TaskResponse, TaskListResponse
from app.services.task_service import TaskService

router = APIRouter()

@router.post("/", response_model=TaskResponse, status_code=201)
async def create_task(
    task_data: TaskCreate,
    current_user: dict = Depends(require_permission(TASK_WRITE))
):
    """创建任务"""
    service = TaskService()
    return await service.create_task(task_data, current_user["user_id"])

@router.get("/", response_model=TaskListResponse)
async def list_tasks(
    status: Optional[str] = None,
    task_type: Optional[str] = None,
    assigned_to: Optional[str] = None,
    current_user: dict = Depends(require_permission(TASK_READ))
):
    """获取任务列表"""
    service = TaskService()
    scope_all = has_permission(current_user, TASK_WRITE)
    return await service.list_tasks(status, task_type, assigned_to, current_user["user_id"], scope_all)

@router.get("/{task_id}", response_model=TaskResponse)
async def get_task(
    task_id: str,
    current_user: dict = Depends(require_permission(TASK_READ))
):
    """获取任务详情"""
    service = TaskService()
    scope_all = has_permission(current_user, TASK_WRITE)
    return await service.get_task_scoped(task_id, current_user["user_id"], scope_all)

@router.post("/{task_id}/assign", response_model=TaskResponse)
async def assign_task(
    task_id: str,
    task_data: TaskAssign,
    current_user: dict = Depends(require_permission(TASK_WRITE))
):
    """手动分配任务：负责人/优先级/截止日期"""
    service = TaskService()
    return await service.assign_task(task_id, task_data, current_user["user_id"])

@router.post("/{task_id}/cancel")
async def cancel_task(
    task_id: str,
    current_user: dict = Depends(require_permission(TASK_WRITE))
):
    """取消任务"""
    service = TaskService()
    return await service.cancel_task(task_id)
```

> 权限接线（RBAC）：创建/分配/取消/暂停/恢复/重试/终止 → `task:write`；列表/详情/进度 → `task:read`。非全局范围仅能访问本人相关任务（数据级权限）。

### 3.5 标注管理 API（annotations.py）

```python
from fastapi import APIRouter, Depends, HTTPException
from app.core.permissions import ANNOTATION_READ, ANNOTATION_WRITE, ANNOTATION_REVIEW, require_permission
from app.schemas.annotation import AnnotationCreate, AnnotationUpdate, AnnotationResponse, ReviewRequest

router = APIRouter()

@router.post("/", response_model=AnnotationResponse, status_code=201)
async def create_annotation(
    annotation: AnnotationCreate,
    current_user: dict = Depends(require_permission(ANNOTATION_WRITE))
):
    """创建标注"""
    pass

@router.get("/document/{document_id}")
async def get_document_annotations(
    document_id: str,
    current_user: dict = Depends(require_permission(ANNOTATION_READ))
):
    """获取文档的所有标注"""
    pass

@router.put("/{annotation_id}", response_model=AnnotationResponse)
async def update_annotation(
    annotation_id: str,
    annotation: AnnotationUpdate,
    current_user: dict = Depends(require_permission(ANNOTATION_WRITE))
):
    """更新标注"""
    pass

@router.post("/{annotation_id}/submit")
async def submit_annotation(
    annotation_id: str,
    current_user: dict = Depends(require_permission(ANNOTATION_WRITE))
):
    """提交标注待审核"""
    pass

@router.post("/{annotation_id}/first-review")
async def first_review_annotation(
    annotation_id: str,
    review: ReviewRequest,
    current_user: dict = Depends(require_permission(ANNOTATION_REVIEW))
):
    """初审标注（submitted → pending_final/rejected）"""
    pass

@router.post("/{annotation_id}/final-review")
async def final_review_annotation(
    annotation_id: str,
    review: ReviewRequest,
    current_user: dict = Depends(require_permission(ANNOTATION_REVIEW))
):
    """终审标注（pending_final → approved/rejected）"""
    pass
```

> 权限接线（RBAC）：创建/更新/提交/删除 → `annotation:write`；列表/详情/版本 → `annotation:read`；初审/终审 → `annotation:review`。

```python
# 标注一致性评估（纯统计，无 DB 依赖）
@router.post("/agreement")
async def annotation_agreement(
    body: AgreementRequest,
    current_user: dict = Depends(require_permission(ANNOTATION_READ)),
):
    """标注一致性评估：Fleiss' Kappa / Cohen's Kappa / Krippendorff's Alpha。"""
    return annotation_agreement_service.evaluate(
        ratings=body.ratings, metrics=body.metrics,
        krippendorff_metric=body.krippendorff_metric,
    )
```

> 标注一致性评估（`annotation_agreement_service.py`）：`cohens_kappa()`（两标注员名义类别）、`fleiss_kappa()`（多标注员名义类别，每样本标注员数可变）、`krippendorff_alpha()`（任意标注员数量，支持 nominal/interval/ratio 度量）；纯 Python 实现零依赖，缺失值自动跳过，接口不变。

### 3.6 操作日志 API（operation_logs.py）

```python
from fastapi import APIRouter, Depends, Query
from typing import Optional
from datetime import datetime
from app.core.security import get_current_user
from app.services.operation_log_service import OperationLogService

router = APIRouter()
operation_log_service = OperationLogService()

@router.get("/", response_model=OperationLogListResponse)
async def list_operation_logs(
    page: int = Query(1, ge=1),
    page_size: int = Query(10, ge=1, le=100),
    user_id: Optional[str] = None,
    action: Optional[str] = None,
    resource_type: Optional[str] = None,
    start_date: Optional[datetime] = None,
    end_date: Optional[datetime] = None,
    current_user: dict = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """查询操作日志，支持按用户/操作类型/资源类型/时间范围筛选"""
    logs, total = await operation_log_service.list_logs(
        db, page, page_size, user_id, action, resource_type, start_date, end_date
    )
    return OperationLogListResponse(items=logs, total=total, page=page, page_size=page_size)
```

**记录逻辑**：`operation_log_service.log_operation()` 在标注服务的 `create_annotation`/`update_annotation`/`submit_annotation`/`first_review`/`final_review` 及删除接口中被调用，`db` 会话由 `get_db` 依赖统一提交，操作日志与业务数据同一事务写入。IP 地址通过 `Request.client.host` 获取。

### 3.7 知识图谱 API（knowledge_graph.py）

```python
from fastapi import APIRouter, Depends, Query
from typing import Optional, List
from app.core.security import get_current_user

router = APIRouter()

@router.get("/search")
async def search_graph(
    query: str,
    entity_type: Optional[str] = None,
    limit: int = 20,
    current_user: dict = Depends(get_current_user)
):
    """搜索知识图谱"""
    pass

@router.get("/entities/{entity_id}")
async def get_entity(
    entity_id: str,
    current_user: dict = Depends(get_current_user)
):
    """获取实体详情"""
    pass

@router.get("/entities/{entity_id}/relations")
async def get_entity_relations(
    entity_id: str,
    depth: int = 1,
    current_user: dict = Depends(get_current_user)
):
    """获取实体关系"""
    pass

@router.post("/query")
async def execute_cypher(
    cypher: str,
    params: dict = None,
    current_user: dict = Depends(get_current_user)
):
    """执行 Cypher 查询"""
    pass

@router.get("/statistics")
async def get_graph_statistics(
    current_user: dict = Depends(get_current_user)
):
    """获取图谱统计信息"""
    pass

@router.post("/entity-link")
async def link_entities(body: EntityLinkRequest, current_user: dict = Depends(get_current_user)):
    """实体链接：Wikidata/DBpedia + 消歧 + 同义词合并（可插拔可降级）"""
    return {"results": entity_linking_service.link_entities(body.entities), "total": 0}

@router.post("/entity-link/single")
async def link_single_entity(body: EntityLinkSingleRequest, current_user: dict = Depends(get_current_user)):
    """链接单个实体"""
    return entity_linking_service.link_entity(body.text, body.entity_type)

@router.post("/reason")
async def relation_reason(body: RelationInferenceRequest, current_user: dict = Depends(get_current_user)):
    """关系推理：组合/逆关系/传递规则"""
    inferred = relation_inference_service.reason(body.triples)
    return {"inferred": inferred, "total": len(inferred)}

@router.post("/link-prediction")
async def link_prediction(body: LinkPredictionRequest, current_user: dict = Depends(get_current_user)):
    """图谱补全：给定头实体与关系预测尾实体（TransE，可降级）"""
    return relation_inference_service.predict_links(body.head, body.relation, body.triples, body.top_k)

@router.post("/triple-score")
async def triple_score(body: TripleScoreRequest, current_user: dict = Depends(get_current_user)):
    """对候选三元组打分（图谱补全缺失边判定）"""
    return {"score": relation_inference_service.score_triple(body.head, body.relation, body.tail, body.triples)}
```

> 实体链接（`entity_linking_service.py`）：`link_entity()`/`link_entities()` 接入 Wikidata（`wbsearchentities`）与 DBpedia（Lookup API），通过编辑距离 + n-gram Jaccard 相似度消歧，内置材料科学领域同义词表（`_DOMAIN_SYNONYMS`）做本地降级；未启用/网络失败自动降级为本地同义词或未链接标记，接口不变。
>
> 关系推理与图谱补全（`relation_inference_service.py`）：`infer_triples()` 规则推理（组合/逆关系/传递，纯函数零依赖）；`TransELinkPredictor`（numpy 实现 TransE 嵌入 + 负采样 SGD）用于链路预测；numpy 缺失/三元组过少降级为 `StatisticalLinkPredictor`（共现 + 邻居 Jaccard），后端 `none` 时仅规则推理，接口不变。

```python
@router.post("/suggest")
async def search_suggest(body: SuggestRequest, current_user: dict = Depends(get_current_user)):
    """语义搜索：搜索建议（前缀/子串/编辑距离自动补全）"""
    return semantic_search_service.suggest(body.prefix, body.candidates, body.limit)

@router.post("/facet-search")
async def facet_search(body: FacetSearchRequest, current_user: dict = Depends(get_current_user)):
    """语义搜索：分面搜索（关键字打分 + 按 facet_key 面计数）"""
    return semantic_search_service.search(body.query, body.records, body.facet_key, body.limit)

@router.post("/text-to-cypher")
async def text_to_cypher(body: TextToCypherRequest, current_user: dict = Depends(get_current_user)):
    """Text-to-Cypher：自然语言 → 只读 Cypher 查询（规则模板，LLM 预留可降级）"""
    result = text_to_cypher_service.translate(body.question, body.node_labels, body.relation_types, body.limit)
    if result["cypher"]:
        ok, msg = text_to_cypher_service.validate(result["cypher"])
        if not ok:
            raise HTTPException(status_code=400, detail=msg)
        if body.execute:
            result["results"] = await graph_service.execute_cypher(result["cypher"], result["params"])
    return result
```

> 语义搜索（`semantic_search_service.py`）：`suggest()` 前缀/子串/编辑距离三级排序自动补全；`search()` 关键字打分（token 重叠 + 子串命中 + 编辑距离）+ 按 `facet_key` 面计数；纯 Python 实现零依赖，Elasticsearch 可选可插拔，接口不变。
>
> Text-to-Cypher（`text_to_cypher_service.py`）：`translate()` 规则模板翻译（统计/列表/关系/关键词四类意图）；`validate()` 只读校验（防 `DELETE`/`CREATE` 等写操作注入，仅允许 `MATCH ... RETURN`）；`TEXT_TO_CYPHER_BACKEND=llm` 预留，未接入时自动降级规则模板，接口不变。

### 3.8 主动学习 API（active_learning.py）

```python
from fastapi import APIRouter, Depends
from pydantic import BaseModel, Field
from typing import Optional
from app.core.security import get_current_user
from app.services.active_learning_service import active_learning_service

router = APIRouter()

# 请求体：samples（含 id/text/probs/features/predictions）、strategy、top_k、*_method
@router.post("/select")
async def select_samples(body: ActiveLearningSelectRequest, current_user: dict = Depends(get_current_user)):
    """主动学习采样：不确定性 / 多样性 / QBC / 混合（可插拔可降级）"""
    return active_learning_service.select(
        samples=body.samples, strategy=body.strategy, top_k=body.top_k,
        uncertainty_method=body.uncertainty_method,
        diversity_method=body.diversity_method, qbc_method=body.qbc_method,
    )
```

> 主动学习（`active_learning_service.py`）：`ActiveLearningService.select()` 门面，策略可插拔可降级；不确定性采样 `uncertainty_score()`（熵/最小置信度/边缘），多样性采样 `diversity_select()`（Core-Set 贪心最远点 / K-Means++ 代表点），QBC `qbc_score()`（投票熵/分歧度）；混合策略 `hybrid` 在可用信号维度加权融合，某维度缺失自动跳过；纯 Python 零依赖，接口不变。

### 3.9 版本管理 API（version_management.py）

```python
from fastapi import APIRouter, Depends
from pydantic import BaseModel, Field
from typing import Any, Optional
from app.core.security import get_current_user
from app.services.version_management_service import version_management_service

router = APIRouter()

# 数据版本：record_dataset / list_datasets
@router.post("/datasets")
async def record_dataset(body: RecordDatasetRequest, current_user: dict = Depends(get_current_user)):
    """记录数据集版本（DVC 风格：内容哈希 + 血缘 + 质量评估快照）"""
    return version_management_service.record_dataset(
        name=body.name, data=body.data, source=body.source,
        pipeline=body.pipeline, annotators=body.annotators, label_key=body.label_key,
    )

# 实验追踪：log_experiment / list_experiments / compare_experiments
@router.post("/experiments")
async def log_experiment(body: LogExperimentRequest, current_user: dict = Depends(get_current_user)):
    """记录实验（MLflow 风格：参数/指标/关联模型版本/标签）"""
    return version_management_service.log_experiment(
        name=body.name, params=body.params, metrics=body.metrics,
        model_name=body.model_name, model_version=body.model_version, tags=body.tags,
    )

# 模型注册：register_model / list_models / transition_stage
@router.post("/models")
async def register_model(body: RegisterModelRequest, current_user: dict = Depends(get_current_user)):
    """注册模型新版本（初始阶段 Staging）"""
    return version_management_service.register_model(
        name=body.name, artifacts=body.artifacts, metrics=body.metrics,
        experiment_id=body.experiment_id, description=body.description,
    )

# 自动重训：check_retrain / run_pipeline / pipeline_status
@router.post("/retrain/check")
async def check_retrain(body: CheckRetrainRequest, current_user: dict = Depends(get_current_user)):
    """自动重训触发条件评估（阈值/定期/手动）"""
    return version_management_service.check_retrain(
        new_annotated=body.new_annotated, last_train_count=body.last_train_count,
        threshold=body.threshold, periodic_days=body.periodic_days,
        last_train_age_days=body.last_train_age_days, force=body.force,
    )
```

> 版本管理（`version_management_service.py`）：`VersionManagementService` 门面，可插拔可降级。数据版本 `record_dataset()`（内容哈希寻址 `dataset_hash()` + 血缘来源/流程/标注员 + 质量评估 `data_quality()` 类别分布/缺失计数）；实验追踪 `log_experiment()`/`compare_experiments()`（多实验指标对比）；模型注册 `register_model()`/`transition_stage()`（Staging → Production → Archived，晋升 Production 自动归档旧版本）；自动重训 `check_retrain()`（阈值/定期/手动触发）/`run_pipeline()`（分段推进 + 失败重试 + 回滚，`RETRAIN_MAX_RETRIES` 控制）；存储默认内置内存注册表，`VERSION_MGMT_STORE_PATH` 可指定 JSON 持久化，DVC/MLflow 作为可选外部后端预留可降级，纯 Python 零依赖，接口不变。

### 3.10 置信度校准与漂移检测 API（calibration_drift.py）

```python
from fastapi import APIRouter, Depends
from pydantic import BaseModel, Field
from typing import Any, Optional
from app.core.security import get_current_user
from app.services.calibration_drift_service import calibration_drift_service

router = APIRouter()

# 置信度校准：temperature / platt
@router.post("/calibrate")
async def calibrate(body: CalibrateRequest, current_user: dict = Depends(get_current_user)):
    """置信度校准：Temperature Scaling / Platt Scaling"""
    return calibration_drift_service.calibrate(
        method=body.method, logits=body.logits, labels=body.labels,
        scores=body.scores, binary_labels=body.binary_labels, temperature=body.temperature,
    )

# 校准质量评估：ECE + 可靠性曲线
@router.post("/evaluate")
async def evaluate_calibration(body: EvaluateCalibrationRequest, current_user: dict = Depends(get_current_user)):
    """校准质量评估：ECE + 可靠性曲线"""
    return calibration_drift_service.evaluate_calibration(body.probs, body.labels, body.n_bins)

# 数据漂移检测：PSI / KS / 卡方
@router.post("/detect")
async def detect_drift(body: DetectDriftRequest, current_user: dict = Depends(get_current_user)):
    """数据漂移检测：PSI / KS / 卡方，阈值分级告警"""
    return calibration_drift_service.detect_drift(
        reference=body.reference, current=body.current,
        method=body.method, categorical=body.categorical,
    )
```

> 置信度校准与漂移检测（`calibration_drift_service.py`）：`CalibrationDriftService` 门面，可插拔可降级。置信度校准 `calibrate()`（`temperature` 温度缩放 `softmax()`/`fit_temperature()` 网格搜索拟合，`platt` 逻辑回归 `fit_platt()`/`platt_calibrate()`）；校准质量 `evaluate_calibration()`（`expected_calibration_error()` ECE + `reliability_curve()` 可靠性直方图）；漂移检测 `detect_drift()`（数值 `psi()`/`ks_statistic()`、类别 `chi_square_drift()`，`classify_psi()` 按 `DRIFT_PSI_WARNING/ALERT` 阈值分级 stable/warning/drift）；纯 Python 零依赖，接口不变。

### 3.11 工作量统计 API（statistics.py）

```python
from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.database import get_db
from app.core.security import get_current_user
from app.services.statistics_service import StatisticsService

router = APIRouter()
statistics_service = StatisticsService()

@router.get("/personal")
async def get_personal_stats(
    current_user: dict = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """当前用户的个人工作量统计（完成量、准确率、任务完成量）。"""
    stats = await statistics_service.get_user_stats(db, current_user["user_id"])
    if stats is None:
        return {  # 用户不存在时返回全零结构
            "user_id": current_user["user_id"], "username": None, "full_name": None, "role": None,
            "annotation_total": 0, "annotation_approved": 0, "annotation_rejected": 0,
            "annotation_pending": 0, "annotation_draft": 0, "accuracy_rate": None,
            "task_total": 0, "task_completed": 0, "task_in_progress": 0, "task_failed": 0,
        }
    return stats

@router.get("/team")
async def get_team_dashboard(
    current_user: dict = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """团队看板：按用户聚合的工作量统计。"""
    return await statistics_service.get_team_dashboard(db)
```

**聚合逻辑**：`statistics_service.py` 基于 `Annotation`（按 `annotated_by` 分组统计各状态数量）和 `Task`（按 `assigned_to` 分组统计各状态数量）进行聚合，并通过 `_build_stats()` 计算：

- `annotation_total` = 各状态标注数量之和
- `annotation_pending` = `submitted` + `pending_final`（待审核）
- `accuracy_rate` = `approved / (approved + rejected) * 100`（审核数不为 0 时）
- `task_in_progress` = `pending` + `running` + `paused`

团队看板按 `annotation_total` 降序排列，返回 `items`（各成员统计）与 `total_users`（成员总数）。

### 3.10 评论讨论 API（comments.py）

```python
from fastapi import APIRouter, Depends, HTTPException, Query, Request
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.database import get_db
from app.core.security import get_current_user
from app.services.comment_service import CommentService
from app.schemas.comment import CommentCreate, CommentResponse, CommentListResponse

router = APIRouter()
comment_service = CommentService()

@router.post("/", response_model=CommentResponse, status_code=201)
async def create_comment(comment: CommentCreate, request: Request,
                         current_user: dict = Depends(get_current_user),
                         db: AsyncSession = Depends(get_db)):
    """对标注结果添加评论，自动解析 @提及 并触发通知。"""
    ip = request.client.host if request.client else None
    result = await comment_service.create_comment(
        db, comment.annotation_id, comment.content, comment.parent_id, current_user["user_id"], ip
    )
    if result is None:
        raise HTTPException(status_code=404, detail="Annotation not found")
    return result

@router.get("/", response_model=CommentListResponse)
async def list_comments(annotation_id: str | None = None, document_id: str | None = None,
                        page: int = Query(1, ge=1), page_size: int = Query(20, ge=1, le=100),
                        current_user: dict = Depends(get_current_user), db: AsyncSession = Depends(get_db)):
    """按标注或文档聚合查询评论。"""
    items, total = await comment_service.list_comments(db, annotation_id, document_id, page, page_size)
    return CommentListResponse(items=items, total=total, page=page, page_size=page_size)

@router.delete("/{comment_id}", status_code=204)
async def delete_comment(comment_id: str, request: Request,
                         current_user: dict = Depends(get_current_user), db: AsyncSession = Depends(get_db)):
    ip = request.client.host if request.client else None
    result = await comment_service.delete_comment(db, comment_id, current_user["user_id"], ip)
    if result is None:
        raise HTTPException(status_code=403, detail="You can only delete your own comments")
    if not result:
        raise HTTPException(status_code=404, detail="Comment not found")
```

**业务逻辑**：`comment_service.create_comment()` 校验标注存在后，用正则 `@([\w\u4e00-\u9fa5.-]+)` 解析评论中的 @ 用户名并匹配真实用户（去重、排除本人），写入 `mentions` JSON 并向被提及用户生成 `mention` 通知；若为回复（`parent_id`），额外向父评论作者生成 `reply` 通知。`delete_comment()` 仅允许本人删除，同时通过 `OperationLogService` 记录操作日志。

### 3.12 通知 API（notifications.py）

```python
from fastapi import APIRouter, Depends, HTTPException, Query
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.database import get_db
from app.core.security import get_current_user
from app.services.notification_service import NotificationService
from app.schemas.comment import NotificationListResponse, UnreadCountResponse

router = APIRouter()
notification_service = NotificationService()

@router.get("/", response_model=NotificationListResponse)
async def list_notifications(page: int = Query(1, ge=1), page_size: int = Query(20, ge=1, le=100),
                             unread_only: bool = False,
                             current_user: dict = Depends(get_current_user), db: AsyncSession = Depends(get_db)):
    items, total, unread = await notification_service.list_notifications(
        db, current_user["user_id"], page, page_size, unread_only
    )
    return NotificationListResponse(items=items, total=total, unread_count=unread, page=page, page_size=page_size)

@router.get("/unread-count", response_model=UnreadCountResponse)
async def get_unread_count(current_user: dict = Depends(get_current_user), db: AsyncSession = Depends(get_db)):
    return UnreadCountResponse(count=await notification_service.unread_count(db, current_user["user_id"]))

@router.post("/{notification_id}/read")
async def mark_read(notification_id: str, current_user: dict = Depends(get_current_user),
                    db: AsyncSession = Depends(get_db)):
    if not await notification_service.mark_read(db, notification_id, current_user["user_id"]):
        raise HTTPException(status_code=404, detail="Notification not found")
    return {"message": "marked as read"}

@router.post("/read-all")
async def mark_all_read(current_user: dict = Depends(get_current_user), db: AsyncSession = Depends(get_db)):
    await notification_service.mark_all_read(db, current_user["user_id"])
    return {"message": "all marked as read"}
```

**业务逻辑**：`notification_service.list_notifications()` 按 `user_id` 查询当前用户通知并返回 `unread_count`；`mark_read()` 仅操作属于当前用户的通知；`mark_all_read()` 将当前用户所有未读通知置为已读。

---

### 3.14 权限管理 API（permissions.py）

```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.database import get_db
from app.core.permissions import (
    PERMISSION_DESCRIPTIONS, ALL_PERMISSIONS, SYSTEM_ROLE_MANAGE,
    get_current_user_permissions, require_permission,
)
from app.services.permission_service import PermissionService
from app.schemas.role import (
    PermissionInfo, PermissionListResponse, MyPermissionsResponse,
    RoleResponse, RoleListResponse, RolePermissionsUpdate,
)

router = APIRouter()
permission_service = PermissionService()

@router.get("/me", response_model=MyPermissionsResponse)
async def get_my_permissions(current_user: dict = Depends(get_current_user_permissions)):
    """返回当前用户的角色及有效权限码列表。"""
    return MyPermissionsResponse(role=current_user.get("role") or "", permissions=current_user.get("permissions") or [])

@router.get("/definitions", response_model=PermissionListResponse)
async def get_permission_definitions(current_user: dict = Depends(get_current_user_permissions)):
    """返回全部可分配权限码及其说明。"""
    items = [PermissionInfo(code=c, description=PERMISSION_DESCRIPTIONS.get(c, "")) for c in ALL_PERMISSIONS]
    return PermissionListResponse(items=items, total=len(items))

@router.get("/roles", response_model=RoleListResponse)
async def list_roles(current_user: dict = Depends(require_permission(SYSTEM_ROLE_MANAGE)), db: AsyncSession = Depends(get_db)):
    """列出全部角色及其权限（仅系统级权限持有者）。"""
    roles = await permission_service.list_roles(db)
    items = [RoleResponse(id=r.id, name=r.name, description=r.description or "", permissions=r.permissions or []) for r in roles]
    return RoleListResponse(items=items, total=len(items))

@router.put("/roles/{role_name}", response_model=RoleResponse)
async def update_role_permissions(role_name: str, body: RolePermissionsUpdate,
                                  current_user: dict = Depends(require_permission(SYSTEM_ROLE_MANAGE)),
                                  db: AsyncSession = Depends(get_db)):
    """更新指定角色的权限（仅系统级权限持有者）。"""
    role = await permission_service.update_role_permissions(db, role_name, body.permissions)
    if not role:
        raise HTTPException(status_code=404, detail="Role not found")
    return RoleResponse(id=role.id, name=role.name, description=role.description or "", permissions=role.permissions or [])
```

**权限核心（core/permissions.py）**：采用 `resource:action` 权限码，三级划分：

| 级别 | 权限码 |
|------|--------|
| 文档级 | `document:read` / `document:write` |
| 任务级 | `task:read` / `task:write` |
| 标注级 | `annotation:read` / `annotation:write` / `annotation:review` |
| 系统级 | `system:user:manage` / `system:role:manage` / `system:audit:read` |

- `get_current_user_permissions()` 读取当前用户角色的 `permissions` JSON 并挂到 `current_user`。
- `has_permission(user, p)`：`role == admin` 或权限含 `all`/`p` 时放行。
- `require_permission(p)` / `require_any_permission(*ps)`：依赖工厂，无权限抛 403。
- 数据级权限：`document_service.get_document`/`list_documents` 与 `task_service.get_task_scoped`/`list_tasks` 增加 `scope_all` 与归属校验（文档上传者、任务负责人/创建人），非全局范围只能访问本人资源。

---

## 四、业务逻辑层

### 4.1 文档服务（document_service.py）

```python
from typing import Optional, List
from fastapi import UploadFile, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select, func
from app.models.document import Document
from app.schemas.document import DocumentCreate
from app.utils.minio_client import MinioClient
from app.workers.parsing_tasks import parse_document_task
from app.core.database import get_db

class DocumentService:
    def __init__(self):
        self.minio_client = MinioClient()
    
    async def upload_document(self, file: UploadFile, title: Optional[str], user_id: str) -> Document:
        """上传文档并存储到 MinIO"""
        file_content = await file.read()
        file_size = len(file_content)
        
        # 上传到 MinIO
        object_name = f"original/{user_id}/{file.filename}"
        self.minio_client.upload_file(object_name, file_content, file.content_type)
        
        # 创建数据库记录
        document = Document(
            title=title or file.filename,
            file_path=object_name,
            file_size=file_size,
            uploaded_by=user_id,
            status="uploaded"
        )
        
        async with get_db() as session:
            session.add(document)
            await session.commit()
            await session.refresh(document)
        
        return document
    
    async def trigger_parsing(self, document_id: str, user_id: str) -> dict:
        """触发解析任务"""
        async with get_db() as session:
            result = await session.execute(select(Document).where(Document.id == document_id))
            document = result.scalar_one_or_none()
            
            if not document:
                raise HTTPException(status_code=404, detail="Document not found")
            
            if document.status == "parsing":
                raise HTTPException(status_code=400, detail="Document is already being parsed")
            
            document.status = "parsing"
            await session.commit()
        
        # 发送 Celery 任务
        task = parse_document_task.delay(document_id)
        
        return {"task_id": task.id, "status": "parsing"}
    
    async def list_documents(self, pagination, status, keyword, user_id) -> dict:
        """获取文档列表"""
        async with get_db() as session:
            query = select(Document).where(Document.uploaded_by == user_id)
            
            if status:
                query = query.where(Document.status == status)
            
            if keyword:
                query = query.where(Document.title.ilike(f"%{keyword}%"))
            
            # 获取总数
            count_query = select(func.count()).select_from(query.subquery())
            total = await session.scalar(count_query)
            
            # 分页
            query = query.offset(pagination.offset).limit(pagination.limit).order_by(Document.created_at.desc())
            result = await session.execute(query)
            documents = result.scalars().all()
            
            return {
                "items": documents,
                "total": total,
                "page": pagination.page,
                "page_size": pagination.limit
            }
```

### 4.2 解析服务（parsing_service.py）

```python
from typing import Dict, List
import fitz  # PyMuPDF
import pdfplumber
from app.utils.minio_client import MinioClient

class ParsingService:
    def __init__(self):
        self.minio_client = MinioClient()
    
    def extract_text_with_pymupdf(self, file_path: str) -> Dict:
        """使用 PyMuPDF 提取文本和元数据"""
        doc = fitz.open(file_path)
        
        result = {
            "metadata": {
                "title": doc.metadata.get("title", ""),
                "author": doc.metadata.get("author", ""),
                "page_count": len(doc)
            },
            "pages": []
        }
        
        for page_num in range(len(doc)):
            page = doc[page_num]
            text = page.get_text()
            
            result["pages"].append({
                "page_number": page_num + 1,
                "text": text,
                "width": page.rect.width,
                "height": page.rect.height
            })
        
        doc.close()
        return result
    
    def extract_tables_with_pdfplumber(self, file_path: str) -> List[Dict]:
        """使用 PDFPlumber 提取表格"""
        tables = []
        
        with pdfplumber.open(file_path) as pdf:
            for page_num, page in enumerate(pdf.pages):
                page_tables = page.extract_tables()
                for table in page_tables:
                    tables.append({
                        "page_number": page_num + 1,
                        "data": table
                    })
        
        return tables
    
    def extract_images(self, file_path: str, output_dir: str) -> List[str]:
        """提取 PDF 中的图像"""
        images = []
        doc = fitz.open(file_path)
        
        for page_num in range(len(doc)):
            page = doc[page_num]
            image_list = page.get_images(full=True)
            
            for img_index, img in enumerate(image_list):
                xref = img[0]
                base_image = doc.extract_image(xref)
                image_bytes = base_image["image"]
                
                image_path = f"{output_dir}/page_{page_num+1}_img_{img_index+1}.{base_image['ext']}"
                with open(image_path, "wb") as f:
                    f.write(image_bytes)
                
                images.append(image_path)
        
        doc.close()
        return images
```

### 4.3 参考文献解析（reference_service.py）

参考文献解析采用「规则 + 正则兜底」方案（未集成 GROBID/Cermine），由 `ParsingService.extract_references()` 委托 `reference_service` 完成：

- **定位区块**：从后向前扫描页面，识别 `References` / `Bibliography` / `参考文献`（含全角空格变体）等 Section 标题，并截断作者简介等后续无关内容。
- **条目切分**：兼容前置序号（`[1] Author. Title...`，英文常见）与后置序号（`Author. Title... ［1］`，中文常见）两种排版。
- **字段抽取**：作者、标题、期刊/会议、年份、卷、期、页码、DOI、文献类型（J/C/M/D/R/P/S/EB-OL）。
- **标准化输出**：`references_to_csl_json()`（CSL-JSON 风格）与 `references_to_bibtex()`。

```python
from app.services.reference_service import (
    reference_service,
    references_to_csl_json,
    references_to_bibtex,
)

refs = reference_service.extract_references("paper.pdf")
# -> [{"index": 1, "authors": "...", "title": "...", "journal": "...",
#      "year": "2020", "volume": "18", "issue": "4", "pages": "467-480",
#      "doi": "10.xxx/...", "type": "journal", "raw": "..."}, ...]

csl = references_to_csl_json(refs)
bib = references_to_bibtex(refs)
```

解析流程（`workers/parsing_tasks.py`）中已接入该步骤，并将结果写入 `document.references`（JSON 字段）。

---

## 五、异步任务（Celery）

### 5.1 Celery 配置（celery_app.py）

```python
from celery import Celery
from app.core.config import settings

celery_app = Celery(
    "worker",
    broker=settings.CELERY_BROKER_URL,
    backend=settings.CELERY_RESULT_BACKEND
)

celery_app.conf.update(
    task_serializer="json",
    accept_content=["json"],
    result_serializer="json",
    timezone="Asia/Shanghai",
    enable_utc=True,
    task_track_started=True,
    task_time_limit=3600,  # 1 小时超时
    task_soft_time_limit=3000,  # 50 分钟软超时
    worker_prefetch_multiplier=1,
    task_acks_late=True,
    task_routes={
        "app.workers.parsing_tasks.*": {"queue": "parsing"},
        "app.workers.extraction_tasks.*": {"queue": "extraction"},
        "app.workers.graph_tasks.*": {"queue": "graph"},
    }
)
```

### 5.2 解析任务（parsing_tasks.py）

```python
from celery import group, chain
from app.core.celery_app import celery_app
from app.services.parsing_service import ParsingService
from app.services.extraction_service import ExtractionService
from app.core.database import get_db
from app.models.document import Document
from app.models.task import Task

@celery_app.task(name="parse_document", bind=True, queue="parsing")
def parse_document_task(self, document_id: str):
    """文档解析任务"""
    parsing_service = ParsingService()
    
    try:
        self.update_state(state="PROGRESS", meta={"progress": 10, "step": "downloading"})
        
        # 1. 下载文件
        # ... 从 MinIO 下载
        
        self.update_state(state="PROGRESS", meta={"progress": 30, "step": "extracting_text"})
        
        # 2. 文本提取
        text_result = parsing_service.extract_text_with_pymupdf(file_path)
        
        self.update_state(state="PROGRESS", meta={"progress": 50, "step": "extracting_tables"})
        
        # 3. 表格提取
        tables = parsing_service.extract_tables_with_pdfplumber(file_path)
        
        self.update_state(state="PROGRESS", meta={"progress": 70, "step": "extracting_images"})
        
        # 4. 图像提取
        images = parsing_service.extract_images(file_path)
        
        self.update_state(state="PROGRESS", meta={"progress": 90, "step": "saving_results"})
        
        # 5. 保存结果到数据库
        # ...
        
        self.update_state(state="PROGRESS", meta={"progress": 100, "step": "completed"})
        
        return {"status": "success", "document_id": document_id}
        
    except Exception as e:
        self.update_state(state="FAILURE", meta={"error": str(e)})
        raise

@celery_app.task(name="extract_entities", bind=True, queue="extraction")
def extract_entities_task(self, document_id: str):
    """实体抽取任务"""
    extraction_service = ExtractionService()
    
    # 调用 LLM 进行实体抽取
    # ...
    
    return {"status": "success", "document_id": document_id}

@celery_app.task(name="build_knowledge_graph", bind=True, queue="graph")
def build_knowledge_graph_task(self, document_id: str):
    """知识图谱构建任务"""
    # 将抽取的实体和关系写入 Neo4j
    # ...
    
    return {"status": "success", "document_id": document_id}
```

---

## 六、应用入口

### 6.1 main.py

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.core.config import settings
from app.api.v1.router import router as api_router
from app.middleware.logging import LoggingMiddleware
from app.utils.neo4j_client import neo4j_driver
from app.utils.redis_client import redis_client

app = FastAPI(
    title=settings.APP_NAME,
    version=settings.APP_VERSION,
    docs_url="/docs",
    redoc_url="/redoc"
)

# CORS 中间件
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "http://localhost:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 日志中间件
app.add_middleware(LoggingMiddleware)

# 注册路由
app.include_router(api_router, prefix=settings.API_PREFIX)

@app.on_event("startup")
async def startup():
    """应用启动时初始化"""
    # 初始化 Neo4j 连接
    await neo4j_driver.verify_connectivity()
    # 初始化 Redis 连接
    await redis_client.ping()

@app.on_event("shutdown")
async def shutdown():
    """应用关闭时清理资源"""
    await neo4j_driver.close()
    await redis_client.close()

@app.get("/health")
async def health_check():
    """健康检查端点"""
    return {"status": "healthy", "version": settings.APP_VERSION}
```

---

## 七、Docker 配置

### 7.1 Dockerfile

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

# 复制依赖文件
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY . .

# 创建非 root 用户
RUN useradd -m appuser && chown -R appuser:appuser /app
USER appuser

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "4"]
```

### 7.2 docker-compose.yml

```yaml
version: "3.8"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=mysql+aiomysql://root:20020522@mysql:3306/wx
      - REDIS_URL=redis://redis:6379/0
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=password
      - MINIO_ENDPOINT=minio:9000
      - CELERY_BROKER_URL=redis://redis:6379/1
      - CELERY_RESULT_BACKEND=redis://redis:6379/2
    depends_on:
      - mysql
      - redis
      - neo4j
      - minio
    volumes:
      - ./moulds:/app/moulds

  celery-worker:
    build: .
    command: celery -A app.core.celery_app worker -l info -Q parsing,extraction,graph
    environment:
      - DATABASE_URL=mysql+aiomysql://root:20020522@mysql:3306/wx
      - REDIS_URL=redis://redis:6379/0
      - NEO4J_URI=bolt://neo4j:7687
      - CELERY_BROKER_URL=redis://redis:6379/1
      - CELERY_RESULT_BACKEND=redis://redis:6379/2
    depends_on:
      - mysql
      - redis
      - neo4j
      - minio
    volumes:
      - ./moulds:/app/moulds
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  mysql:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=20020522
      - MYSQL_DATABASE=wx
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  neo4j:
    image: neo4j:5
    environment:
      - NEO4J_AUTH=neo4j/password
      - NEO4J_PLUGINS=["apoc"]
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
    ports:
      - "7474:7474"
      - "7687:7687"

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
    volumes:
      - minio_data:/data
    ports:
      - "9000:9000"
      - "9001:9001"

volumes:
  mysql_data:
  redis_data:
  neo4j_data:
  neo4j_logs:
  minio_data:
```

---

## 八、requirements.txt

```
# Web 框架
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# 数据库
sqlalchemy[asyncio]==2.0.25
aiomysql==0.2.0
alembic==1.13.1
pymysql==1.1.0

# Redis
redis==5.0.1

# Neo4j
neo4j==5.16.0

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Celery
celery==5.3.6
flower==2.0.1

# 文件存储
minio==7.2.3
boto3==1.34.14

# PDF 处理
PyMuPDF==1.23.8
pdfplumber==0.10.3

# AI/ML
transformers==4.37.0
torch==2.1.2
sentence-transformers==2.2.2
faiss-cpu==1.7.4

# 数据验证
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# 工具
httpx==0.26.0
python-dotenv==1.0.0
loguru==0.7.2
```

---

## 九、开发指南

### 9.1 本地开发环境搭建

```bash
# 1. 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 2. 安装依赖
pip install -r requirements.txt

# 3. 配置环境变量
cp .env.example .env
# 编辑 .env 文件

# 4. 启动数据库服务
docker-compose up -d mysql redis neo4j minio

# 5. 初始化数据库
alembic upgrade head

# 6. 启动开发服务器
uvicorn app.main:app --reload --port 8000

# 7. 启动 Celery Worker
celery -A app.core.celery_app worker -l info -Q parsing,extraction,graph
```

### 9.2 API 文档

启动服务后访问：
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### 9.3 测试

```bash
# 运行所有测试
pytest

# 运行特定测试
pytest tests/test_auth.py

# 生成覆盖率报告
pytest --cov=app --cov-report=html
```

### 9.4 代码规范

```bash
# 代码格式化
black app/

# 代码检查
ruff check app/

# 类型检查
mypy app/
```
