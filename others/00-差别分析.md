# 多模态科学文献智能解析与知识图谱构建平台 - 项目差别分析

## 文档信息

- **分析日期**: 2026-05-13 (更新: 2026-05-20)
- **分析依据**: XiangMu.md（立项文档）、项目实际代码

---

## 一、整体完成度概览

| 模块 | 立项计划 | 实际完成 | 完成度 | 状态 |
|------|---------|---------|--------|------|
| Phase 1: 基础架构 | 4 项 | 4 项 | 100% | ✅ 完成 |
| Phase 2: 文献解析 | 4 项 | 4 项 | 100% | ✅ 完成 |
| Phase 3: 信息抽取 | 4 项 | 4 项 | 100% | ✅ 完成 |
| Phase 4: 前端平台 | 4 项 | 4 项 | 100% | ✅ 完成 |
| Phase 5: 知识图谱 | 4 项 | 4 项 | 100% | ✅ 完成 |
| Phase 6: 系统优化 | 4 项 | 1 项 | 25% | ⚠️ 部分完成 |

**总体完成度**: 约 **80%**

---

## 更新记录 (2026-05-20)

| 功能 | 状态 | 说明 |
|------|------|------|
| 用户注册功能 | ✅ 新增 | 登录页添加注册按钮和弹窗 |
| 知识图谱示例数据 | ✅ 新增 | 添加钙钛矿太阳能电池领域10个实体和10条关系 |
| 图谱探索增强 | ✅ 增强 | 添加统计卡片、类型筛选、节点详情抽屉 |
| 初始化示例数据按钮 | ✅ 新增 | 前端一键初始化Neo4j示例数据 |

---

## 更新记录 (2026-09-06)

| 功能 | 状态 | 说明 |
|------|------|------|
| MinIO 部署接入 | ✅ 完成 | 本机 9000/9001，上传/下载/删除验证通过 |
| 依赖服务验证 | ✅ 完成 | MySQL/Redis/Neo4j 连通验证，`init_db.py` 初始化通过 |
| 上传→解析→结果入库闭环 | ✅ 完成 | `test/test_upload_parse_loop.py` 跑通 |
| 基础单元测试 | ✅ 完成 | pytest 10 用例（document/parsing/auth service） |

---

## 更新记录 (2026-09-07)

| 功能 | 状态 | 说明 |
|------|------|------|
| 向量库与嵌入模型 | ✅ 完成 | 新增 `GraphRAGTest/vector_store.py`（FAISS + BGE-M3，降级为哈希+numpy） |
| 混合检索 + 重排序 | ✅ 完成 | 新增 `GraphRAGTest/hybrid_retrieval.py`（向量 + BM25 + RRF，可选 Cross-Encoder 精排） |
| sentence-transformers 真实集成 | ✅ 完成 | 安装 torch 2.14.0+cpu + sentence-transformers 6.0.1，下载 BGE-M3 + bge-reranker，真实嵌入(dim=1024)+精排(cross-encoder)跑通 |
| 打通索引管线 | ✅ 完成 | `hybrid_retrieval.py` 新增 `index_document_vectors()`；`indexer.py` 增加 `chunks` 入 Neo4j + 向量库；新增 `POST /hybrid/search`；后端 `chunks` 透传；`test/test_index_pipeline.py` 2 用例通过 |
| 公式识别（LaTeX-OCR） | ✅ 完成 | `formula_service.py` 封装 pix2tex，公式图 → LaTeX；`test/test_formula_service.py` 4 用例通过 |
| 页眉页脚处理 | ✅ 完成 | `layout_service.py` 新增 `detect_header_footer`（纵向位置启发式）+ `is_page_number`；`parsing_service.extract_page_elements()` 打通 OCR→版面→页眉页脚 |
| 模型路径统一到 moulds | ✅ 完成 | 新增 `moulds/`（分模型子文件夹）；`resolve_model_source()` 本地目录优先、缺失回退 HF 名 |
| 图表理解（YOLOv8 + BLIP-2） | ✅ 完成 | `figure_detection_service.py`（YOLOv8 图表/公式检测）+ `chart_description_service.py`（BLIP-2 描述），`extract_figures()` 打通；`test_figure_detection_service` 3 + `test_chart_description_service` 2 用例通过 |
| NER（SciBERT 微调） | ✅ 完成 | `ner_service.py` 封装微调 SciBERT token classification，`extraction_service.extract_entities()` 增强；`test_ner_service.py` 3 用例通过 |
| 代码审查 & Pydantic V2 迁移 | ✅ 完成 | 修复 `vector_store.get_dim` 静默降级、`extraction_service` 缺 `DocumentPage` 导入、页眉页脚像素/点单位混用；`class Config`→`model_config`（7 文件）消除 Pydantic V2 弃用警告；补装 `pytest-asyncio`/`aiosqlite`，全量测试 44 passed |

---

## 更新记录 (2026-09-08)

| 功能 | 状态 | 说明 |
|------|------|------|
| RBAC 细粒度权限 | ✅ 完成 | `core/permissions.py` 权限码 + `require_permission` 依赖工厂；`permission_service.py`/`permissions.py` 角色权限管理；文档/任务级归属校验；用户/操作日志改系统级权限码；前端动态菜单 + 路由守卫 + `RolePermissionPage` |

---

## 更新记录 (2026-09-09)

| 功能 | 状态 | 说明 |
|------|------|------|
| 实体链接 | ✅ 完成 | 新增 `entity_linking_service.py`（Wikidata/DBpedia 查询 + 消歧 + 同义词合并，内置领域同义词表本地降级）；`config.py` 新增 `ENTITY_LINKING_*` 配置；`knowledge_graph.py` 新增 `POST /knowledge-graph/entity-link`（批量）/`entity-link/single`；`test_entity_linking_service.py` 10 用例通过 |
| 语义搜索升级 | ✅ 完成 | 新增 `semantic_search_service.py`（分面搜索 + 搜索建议，纯 Python 零依赖）；`config.py` 新增 `SEMANTIC_SEARCH_ENABLED`；`knowledge_graph.py` 新增 `POST /knowledge-graph/suggest`/`facet-search`；`test_semantic_search_service.py` 8 用例通过 |
| Text-to-Cypher | ✅ 完成 | 新增 `text_to_cypher_service.py`（规则模板翻译 + 只读校验）；`config.py` 新增 `TEXT_TO_CYPHER_*` 配置；`knowledge_graph.py` 新增 `POST /knowledge-graph/text-to-cypher`；`test_text_to_cypher_service.py` 8 用例通过 |
| 主动学习策略 | ✅ 完成 | 新增 `active_learning_service.py`（不确定性/多样性/QBC/混合采样，纯 Python 零依赖）；`config.py` 新增 `ACTIVE_LEARNING_*`；新增 `POST /active-learning/select` 并注册 `router.py`；`test_active_learning_service.py` 11 用例通过 |
| 标注一致性评估 | ✅ 完成 | 新增 `annotation_agreement_service.py`（Cohen's Kappa / Fleiss' Kappa / Krippendorff's Alpha，纯 Python 零依赖）；`config.py` 新增 `ANNOTATION_AGREEMENT_ENABLED`；`annotations.py` 新增 `POST /annotations/agreement`；`test_annotation_agreement_service.py` 11 用例通过 |
| 数据/模型版本管理 | ✅ 完成 | 新增 `version_management_service.py`（DVC 数据版本 + MLflow 实验追踪/模型注册 + 自动重训管道 + `run_finetune_pipeline` 真实微调闭环，纯 Python 零依赖可降级）；`finetune_service.py`（LoRA/QLoRA 微调 + 多数类基线降级 + token 级 F1/P/R）；`config.py` 新增 `VERSION_MGMT_*`/`RETRAIN_*` 配置；`version_management.py` 新增 `/version-management/*` 接口（含 `/retrain/finetune`）并注册 `router.py`；`test_version_management_service.py` + `test_finetune_service.py` 用例通过 |
| 置信度校准与漂移检测 | ✅ 完成 | 新增 `calibration_drift_service.py`（Temperature/Platt Scaling + ECE/可靠性曲线 + PSI/KS/卡方漂移告警，纯 Python 零依赖）；`config.py` 新增 `CALIBRATION_DRIFT_*`/`DRIFT_PSI_*` 配置；`calibration_drift.py` 新增 `POST /calibration-drift/{calibrate,evaluate,detect}` 并注册 `router.py`；`test_calibration_drift_service.py` 17 用例通过 |

---

## 更新记录 (2026-09-10)

| 功能 | 状态 | 说明 |
|------|------|------|
| 后端测试补全 | ✅ 完成 | pytest `321 passed`，语句覆盖率 **87%**；补齐 Celery 任务（`test/test_workers_tasks.py`）与各 AI/知识图谱服务测试；`test/` 目录统一存放测试代码 |
| 前端测试补全 | ✅ 完成 | Vitest + RTL `34` 测试文件、`138` 用例全部通过，语句覆盖率 **87.66%**；覆盖 stores/hooks/api/router 及主要页面/组件 |
| E2E 测试 | ✅ 完成 | Playwright 已配置（`playwright.config.ts` + `e2e/smoke.spec.ts` 冒烟用例），新增 `test:coverage`/`test:e2e` 脚本 |
| 测试问题修复 | ✅ 完成 | 修复 `AnnotationHistory.tsx` 缺失 `Space` 导入、vitest 覆盖率配置排除测试文件、`extract_build_knowledge_graph_stub` 任务重名冲突、antd 文本空白匹配等 |
| 代码质量工具 | ✅ 完成 | 后端新增 `pyproject.toml`（Ruff+Black，FastAPI/自研依赖加入 `extend-immutable-calls`）+ `requirements-dev.txt`（ruff==0.12.0/black==25.9.0）；前端新增 `.eslintrc.cjs` + `.prettierrc` + `.prettierignore`；修复 `GraphExplorer.tsx` Hooks 条件调用等 lint 问题；`ruff check`/`black --check`/`eslint`/`prettier --check` 全部通过 |
| 参考文献解析与结构化 | ✅ 完成 | 新增 `reference_service.py`（Section 定位 + 前置/后置序号切分 + 作者/标题/期刊/年份/卷期页/DOI/类型字段抽取 + CSL-JSON/BibTeX 标准化）；`parsing_service.extract_references()` 委托；`parsing_tasks.py` 解析流程写入 `document.references`；`Document` 模型 + schema + 前端 `DocumentDetailPage` 参考文献列表；`test_reference_service.py` 6 用例通过 |

## 更新记录 (2026-09-15)

| 功能 | 状态 | 说明 |
|------|------|------|
| REBEL / OpenIE 关系抽取模型 | ✅ 完成 | 新增 `rebel_service.py`（`parse_rebel_output` 纯函数解析 seq2seq 输出 + `normalize_relation_type` 规范化为 SCREAMING_SNAKE + `RebelService` 懒加载/可降级，基于 `Babelscape/rebel-large`）；`config.py` 新增 `RELATION_EXTRACTION_ENABLED`/`REBEL_MODEL`/`REBEL_MODEL_PATH` 等配置；`extraction_service.py` 新增 `_extract_relations_by_rebel()` 并对规则+REBEL 结果去重合并、与 NER 实体对齐 source_type/target_type；`test_rebel_service.py` 7 用例通过 |

## 更新记录 (2026-09-20)

| 功能 | 状态 | 说明 |
|------|------|------|
| 文档/代码一致性核对与修正 | ✅ 完成 | 全量核对 `others` 文档与实际代码：config 配置项、15 个 API 模块、31 个服务、models/schemas/workers、前端页面均一致，无「文档宣称完成但代码缺失」项；修正 `00-差别分析.md` Phase 5 关系推理口径、`PROJECT_SUMMARY.md` 章节指针（第十一章→第七章）与过时数量（7→31 服务 / 7→15 模块）、`02-后端开发文档.md` 目录树（补 3 个 API 模块 + role/system_config 模型 + 16 个 AI/高级服务 + database/logging 核心模块 + metrics 工具，修正 middleware CORS 位置与 tests→storage）、`开发报告.md` 目录树数量 |

---

## 二、详细差别分析

### Phase 1: 基础架构搭建 ✅ 完成

| 计划内容 | 实际状态 | 说明 |
|---------|---------|------|
| 前后端项目初始化 | ✅ 已完成 | React + FastAPI 项目结构完整 |
| 数据库 Schema 设计 | ✅ 已完成 | MySQL 表结构定义完整 |
| 用户认证系统 | ✅ 已完成 | JWT 认证、登录注册 |
| CI/CD 流程搭建 | ⚠️ 部分完成 | Docker 配置存在，CI/CD 未配置 |

---

### Phase 2: 文献解析模块 ✅ 完成

| 计划内容 | 实际状态 | 说明 |
|---------|---------|------|
| PDF 解析服务 | ✅ 完成 | PyMuPDF + pdfplumber；MinIO 已部署并验证（上传/下载/删除） |
| 版面分析模型集成 | ✅ 完成 | LayoutLMv3 可插拔接入（`layout_service.py`），缺模型自动降级；页眉页脚启发式判定 |
| 公式识别服务 | ✅ 完成 | LaTeX-OCR（pix2tex）可插拔接入（`formula_service.py`），缺模型自动降级 |
| 图表检测与提取 | ✅ 完成 | YOLOv8 可插拔检测（`figure_detection_service.py`）+ BLIP-2 描述（`chart_description_service.py`），缺模型自动降级 |
| 参考文献解析与结构化 | ✅ 完成 | `reference_service.py` 规则 + 正则兜底（Section 定位/条目切分/字段抽取/CSL-JSON+BibTeX） |

#### 详细分析

**已完成部分**:
- `parsing_service.py` - PDF 文本提取框架
- `pdfplumber` 集成 - 表格提取
- 图像提取功能
- OCR 识别（PaddleOCR 可插拔接入）：`ocr_service.py` + `parsing_service.py` 扫描件/图片页 OCR 回退
- 版面分析（LayoutLMv3 可插拔接入）：`layout_service.py`，输出 title/paragraph/list/table/figure 元素 + bbox
- 页眉页脚处理：`layout_service.py` 的 `detect_header_footer`（纵向位置启发式 + 页码识别）
- 公式识别（LaTeX-OCR/pix2tex 可插拔接入）：`formula_service.py`，公式图 → LaTeX
- 图表/公式检测（YOLOv8 可插拔接入）：`figure_detection_service.py`
- 图表描述（BLIP-2 可插拔接入）：`chart_description_service.py`；`parsing_service.extract_figures()` 打通检测→识别/描述
- 参考文献解析（`reference_service.py`）：Section 定位（正则 + 版面）+ 前置/后置序号切分 + 字段抽取 + CSL-JSON/BibTeX 标准化，`parsing_tasks.py` 写入 `document.references`
- 结果保存到数据库

**未完成部分**:
- （无，本阶段功能已全部完成，含参考文献解析与结构化）

**依赖问题**:
- ✅ MinIO 服务已部署并验证（本机 9000/9001，上传/下载/删除通过）

---

### Phase 3: 信息抽取模块 ✅ 完成

| 计划内容 | 实际状态 | 说明 |
|---------|---------|------|
| RAG 框架搭建 | ✅ 已完成 | GraphRAG 本地实现 |
| LLM 服务部署 | ✅ 已完成 | LM Studio + qwen/qwen3-4b-2507 |
| 实体关系抽取 | ✅ 完成 | `extraction_service.py` 规则 + NER（`ner_service.py`，微调 SciBERT）+ REBEL（`rebel_service.py`，关系分类），均可插拔降级 |
| 向量数据库集成 | ✅ 已完成 | FAISS + BGE-M3（vector_store.py，降级为哈希+numpy） |

#### 详细分析

**已完成部分**:
- ✅ GraphRAGTest 子系统（Neo4j + LM Studio）
- ✅ `extraction_service.py` - 实体/关系抽取框架（规则 + NER + REBEL，LLM 预留）
- ✅ `ner_service.py` - SciBERT 微调 NER（可插拔降级），`extract_entities()` 融合 NER + 规则
- ✅ `rebel_service.py` - REBEL/OpenIE 关系分类（可插拔降级），`extract_relations()` 融合规则 + REBEL 并去重
- ✅ `lmstudio_client.py` - LLM 调用
- ✅ `graphrag_service.py` - GraphRAG 问答流程
- ✅ `vector_store.py` - FAISS + BGE-M3 向量存储与相似度检索（可降级为哈希+numpy）
- ✅ `hybrid_retrieval.py` - 向量 + BM25 混合检索与 RRF 融合（可选 Cross-Encoder 精排）

**未完成部分**:
- （本阶段功能已全部完成）

**当前实现**:
- 实体抽取融合 SciBERT NER（模型可用时优先，缺省回退规则）；关系抽取融合 REBEL 关系分类（模型可用时补充，缺省回退规则/LLM）
- 图谱检索使用 Neo4j 模糊查询

---

### Phase 4: 前端平台开发 ✅ 完成

| 计划内容 | 实际状态 | 说明 |
|---------|---------|------|
| 文档管理页面 | ✅ 已完成 | 上传、列表、详情、删除 |
| PDF 查看器与双栏对比 | ✅ 已完成 | PDF.js 集成，双栏视图 |
| 标注工作台 | ✅ 已完成 | 实体标注、关系标注 |
| 知识图谱可视化 | ✅ 已完成 | ForceGraph 可视化 |

#### 详细分析

**已完成部分**:
- ✅ 所有页面组件（9个页面）
- ✅ PDF 查看器（双栏对比）
- ✅ 任务管理（含进度跟进）
- ✅ 标注工作台
- ✅ 知识图谱可视化
- ✅ 状态管理（Zustand）
- ✅ API 接口层完整

**额外完成**:
- ✅ GraphRAG 问答页面
- ✅ 重新解析功能
- ✅ 用户注册功能 (登录页注册弹窗)

---

### Phase 5: 知识图谱构建 ✅ 完成

| 计划内容 | 实际状态 | 说明 |
|---------|---------|------|
| Neo4j 图谱存储 | ✅ 已完成 | GraphRAGTest 子系统 |
| 知识融合与对齐 | ✅ 已完成 | 实体链接（Wikidata/DBpedia + 消歧 + 同义词合并，可降级） |
| 语义搜索功能 | ✅ 已完成 | 分面搜索 + 搜索建议（纯 Python，零依赖，可插拔 ES；混合检索由 GraphRAGTest 提供） |
| 智能问答系统 | ✅ 已完成 | GraphRAG + Qwen + Text-to-Cypher（自然语言 → 只读 Cypher） |

#### 详细分析

**已完成部分**:
- ✅ Neo4j 连接与操作
- ✅ 节点/关系 CRUD
- ✅ GraphRAG 问答流程
- ✅ 图谱统计
- ✅ 文档索引
- ✅ 示例数据初始化 (seed_data.py + API)
- ✅ 图谱探索前端增强 (统计卡片、筛选、详情抽屉)

**未完成部分**:
- ⚠️ RotatE 关系推理（当前以规则推理 + TransE 链路预测替代）
- ❌ GNN 图神经网络链路预测（当前以 TransE 链路预测 + 统计降级替代）

---

### Phase 6: 系统优化与部署 ⚠️ 部分完成

| 计划内容 | 实际状态 | 说明 |
|---------|---------|------|
| 主动学习采样策略 | ⚠️ 部分完成 | 不确定性/多样性/QBC 采样已实现（`active_learning_service.py`） |
| 性能优化 | ❌ 未开始 | 无 |
| 监控告警配置 | ⚠️ 部分完成 | 告警/业务指标代码侧已打通：`business_metrics.py`（/metrics/business，模型推理延迟 + Celery 队列长度）、`alert_service.py` + `/alerts/webhook`（钉钉/企业微信/通用渠道转发）；Prometheus/Grafana/ELK 配置已提供。本机未安装 Docker，监控栈（4.1/4.2）未实际运行 |
| 生产环境部署 | ⚠️ 部分完成 | Docker Compose 存在 |

#### 待实现功能

**主动学习管道**:
- ✅ 采样策略已完成（不确定性 / 多样性 / QBC，见 `active_learning_service.py`）
- ✅ 标注一致性评估已完成（Fleiss' Kappa / Cohen's Kappa / Krippendorff's Alpha，见 `annotation_agreement_service.py`）
- ✅ 数据/模型版本管理已完成（DVC 数据版本 + MLflow 实验追踪/模型注册 + 自动重训管道，见 `version_management_service.py`）
- ✅ 置信度校准与漂移检测已完成（Temperature/Platt Scaling + ECE + PSI/KS/卡方漂移告警，见 `calibration_drift_service.py`）

**监控告警**:
- ✅ Prometheus 指标收集（后端 `/metrics` + 业务指标 `/metrics/business`，`prometheus.yml` 双 job）
- ✅ Grafana 仪表盘（provisioning + backend-overview，新增模型推理延迟/Celery 队列长度面板）
- ✅ 告警接入（`alert_service.py` 格式化/加签 + `/alerts/webhook` 接收转发 + `alertmanager.yml` 指向后端 webhook；`alerts.yml` 慢推理/队列积压规则）
- ✅ ELK 日志聚合（后端 JSON 日志 + Filebeat + Logstash，告警以结构化日志 `event:"alert"` 进入 Kibana 检索）
- ⚠️ 端到端运行：本机未安装 Docker，监控栈容器未启动（详见 XiangMu.md 步骤 4 的 Docker 验证命令）

---

## 三、未完成的详细清单

### 3.1 AI 模型集成（高优先级）

| 模型/工具 | 计划用途 | 当前状态 | 优先级 |
|----------|---------|---------|--------|
| PyMuPDF | PDF 解析 | ✅ 已集成 | - |
| PDFPlumber | 表格提取 | ✅ 已集成 | - |
| LayoutLMv3 | 版面分析 | ✅ 已集成（可插拔降级） | 高 |
| YOLOv8 | 图表/公式检测 | ✅ 已集成（可插拔降级） | 高 |
| LaTeX-OCR | 公式识别 | ✅ 已集成（可插拔降级） | 中 |
| PaddleOCR | 扫描 PDF / 图片文字页 | ✅ 已集成（可插拔降级） | 中 |
| BLIP-2 | 图表描述 | ✅ 已集成（可插拔降级） | 低 |
| SciBERT | NER | ✅ 已集成（可插拔降级，微调 checkpoint） | 高 |
| BGE-M3 | 向量嵌入 | ✅ 已集成 | 中 |
| FAISS/ChromaDB | 向量检索 | ✅ 已集成（FAISS，可降级） | 中 |

### 3.2 高级功能（中等优先级）

| 功能 | 说明 | 当前状态 |
|------|------|---------|
| 混合检索 | 向量 + BM25 | ✅ 已实现 |
| Cross-Encoder 重排序 | 检索结果精排 | ✅ 已实现（可选可降级） |
| 实体链接 | Wikidata/DBpedia | ✅ 已实现（可插拔可降级） |
| 关系推理 | TransE/RotatE | ✅ 已实现（规则推理 + TransE，可降级） |
| 知识图谱补全 | GNN 链路预测 | ✅ 已实现（TransE 链路预测 + 统计降级） |
| 语义搜索 | 分面搜索 + 搜索建议 | ✅ 已实现（纯 Python 零依赖，可插拔 ES） |
| Text-to-Cypher | 自然语言转 Cypher（GraphQA） | ✅ 已实现（规则模板，LLM 预留可降级） |
| 主动学习采样 | 不确定性/多样性/QBC | ✅ 已实现（纯 Python 零依赖） |
| LoRA/QLoRA 微调 | 科学领域适配 | ✅ 已实现（`finetune_service.py`，PEFT 微调 + 多数类基线降级，接入版本管理闭环） |

### 3.3 平台功能（中低优先级）

| 功能 | 说明 | 当前状态 |
|------|------|---------|
| RBAC 权限 | 细粒度权限控制 | ✅ 已实现（文档级/任务级/系统级权限码 + 归属校验） |
| 任务分配 | 多人协作分配 | ✅ 已实现 |
| 主动学习推送 | 低置信度样本优先 | ❌ 未实现 |
| 评论讨论 | @提及、通知 | ✅ 已实现 |
| 多级审核 | 标注 → 初审 → 终审 | ✅ 已实现 |
| 操作日志 | 标注/修改/审核审计，按用户/时间/类型筛选 | ✅ 已实现 |
| 实时协作 | WebSocket | ❌ 未实现 |
| 工作量统计 | 个人/团队看板 | ✅ 已实现（个人完成量/准确率 + 团队看板） |

### 3.4 运维功能（低优先级）

| 功能 | 说明 | 当前状态 |
|------|------|---------|
| Prometheus 监控 | 指标收集 | ⚠️ 已配置（后端 `/metrics` + 业务指标 `/metrics/business` + `prometheus.yml` 双 job） |
| Grafana 仪表盘 | 可视化监控 | ⚠️ 已配置（provisioning + backend-overview 仪表盘，含模型推理延迟/Celery 队列长度面板） |
| 告警接入 | 告警分发 | ⚠️ 已配置（`alert_service.py` + `/alerts/webhook` 接收转发 + `alertmanager.yml` → 后端 webhook） |
| ELK 日志聚合 | 日志分析 | ⚠️ 已配置（后端 JSON 日志 + Filebeat + Logstash，告警结构化日志可检索） |
| CI/CD 流水线 | 自动部署 | ⚠️ 已配置 CI 工作流（自动部署未实现） |
| A/B 测试框架 | 模型对比 | ❌ 未实现 |

---

## 四、当前可正常运行的功能

### 后端服务
- ✅ 用户认证（登录、注册、JWT）
- ✅ 文档 CRUD
- ✅ 任务管理（创建、查询、暂停、恢复、终止、重试）
- ✅ 标注 CRUD
- ✅ 操作日志（记录标注/修改/审核，按用户/类型/时间筛选）
- ✅ 知识图谱 CRUD
- ✅ GraphRAG 问答
- ✅ Celery 异步任务

### 前端页面
- ✅ 登录/注册
- ✅ 仪表盘
- ✅ 文档管理（上传、列表、详情）
- ✅ 任务管理（列表、详情、进度）
- ✅ 标注工作台
- ✅ 操作日志查询页
- ✅ 工作量统计页
- ✅ 知识图谱可视化
- ✅ GraphRAG 问答
- ✅ 个人设置

### 服务依赖
- ✅ MySQL 数据库（本机 3306，运行中）
- ✅ Redis（本机 6379，PING → PONG）
- ✅ Neo4j（本机 7474/7687，bolt 认证 `neo4j/wang245484@` 已验证）
- ✅ MinIO（本机 9000/9001，已部署并验证上传/下载/删除）

---

## 五、建议的后续开发路径

### 短期目标（1-2 周）

1. **解决 MinIO 依赖** ✅（已完成，MinIO 已部署并验证）
   - ✅ 部署 MinIO 服务（本机 minio.exe，9000/9001）
   - ✅ `minio_client.py` 上传/下载/删除验证通过（2026-09-06）

2. **完善任务进度功能**
   - 测试 Celery 任务进度同步
   - 添加进度步骤描述

3. **完善 PDF 解析流程**
   - 测试完整解析流程
   - 添加示例 PDF

### 中期目标（1 个月）

1. **AI 模型集成**
   - 集成 LayoutLMv3 版面分析
   - 集成 SciBERT NER
   - ✅ 集成 BGE-M3 向量嵌入（已完成，见 vector_store.py）

2. **增强知识图谱**
   - ✅ 添加实体链接功能（已完成，见 entity_linking_service.py）
   - ✅ 实现关系推理 / 图谱补全（已完成，见 relation_inference_service.py）

3. **平台增强**
   - 任务分配系统
   - 工作量统计

### 长期目标（持续）

1. **主动学习管道**
2. **监控告警**
3. **生产部署**

---

## 六、附录：文件清单

### 后端核心文件
```
backend/
├── app/
│   ├── api/v1/           # 15个 API 模块 ✅
│   ├── core/              # 配置、安全、Celery ✅
│   ├── models/            # 数据库模型 ✅
│   ├── schemas/           # Pydantic 模型 ✅
│   ├── services/          # 30+ 个业务服务 ✅
│   ├── workers/           # 3个 Celery 任务 ✅
│   └── utils/             # 工具类 ✅
```

### 前端核心文件
```
frontend/src/
├── api/                   # API 接口层 ✅
├── components/            # 6个核心组件 ✅
├── pages/                 # 9个页面 ✅
├── store/                 # 3个状态管理 ✅
├── types/                 # TypeScript 类型 ✅
└── router/               # 路由配置 ✅
```

### 独立子系统
```
GraphRAGTest/              # GraphRAG 问答系统 ✅
├── app.py
├── neo4j_client.py
├── lmstudio_client.py
├── graphrag_service.py
├── indexer.py
└── api/routes.py
```

---

**文档版本**: v1.0
**维护者**: Claude Code
