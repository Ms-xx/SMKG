# 环境配置文档

## 概述

本文档列出了开发和部署多模态科学文献智能解析平台所需的所有环境、依赖和服务。

---

## 〇、本机环境检测结果

> 检测时间: 2026-05-12
> 操作系统: Windows 11 Pro 10.0.26200

### 已安装的工具

| 工具 | 要求版本 | 本机版本 | 状态 | 备注 |
|------|----------|----------|------|------|
| Python | 3.11.x | **3.14.3** | ✅ 已安装 | 版本偏高，位于 `C:\Users\wly\AppData\Local\Python\bin\` |
| Node.js | 20.x LTS | **v24.14.0** | ✅ 已安装 | 版本偏高，位于 `D:\workapp\Node\` |
| npm | - | **11.9.0** | ✅ 已安装 | 随 Node.js 安装 |
| pnpm | 8.x+ | **10.33.0** | ✅ 已安装 | 版本高于要求，通过 `npm install -g pnpm` 安装 |

### 未安装的工具/服务

| 工具/服务 | 要求版本 | 状态 | 安装优先级 | 安装方式 |
|-----------|----------|------|------------|----------|
| Git | 2.40+ | **2.53.0.windows.2** | ✅ 已安装 | 通过官网安装 |
| Docker | 24.x+ | ❌ 未安装 | 高 | Docker Desktop for Windows |
| Docker Compose | 2.x+ | ❌ 未安装 | 高 | 随 Docker Desktop 安装 |
| MySQL | 8.0.x | ✅ 已部署 | 中 | 本地 localhost:3306 |
| Redis | 7.x | ❌ 未部署 | 中 | Docker 或本地安装 |
| Neo4j | 5.x | ❌ 未部署 | 中 | Docker 或本地安装 |
| MinIO | latest | ❌ 未部署 | 低 | Docker 安装 |
| NVIDIA GPU/CUDA | - | ❌ 不可用 | 低 | 需要 NVIDIA 显卡 + 驱动 |

### 已安装的 Python 包 (仅 Jupyter 相关)

本机默认 Python（3.14.3）当前主要为 Jupyter/IPython 生态，**项目后端运行所需包大部分仍未安装**（torch、transformers、PyMuPDF、Celery、uvicorn、aiomysql、alembic 等）：
- asttokens, colorama, debugpy, ipykernel, ipython, jupyter_client 等
- 注：为完成 MinIO 接入与数据库初始化验证，已补装最小依赖 `minio`、`pydantic-settings`、`sqlalchemy(2.0.52)`、`fastapi`、`python-jose`、`passlib`、`bcrypt(4.0.1)`、`neo4j`。
- 注（单元测试）：为运行 `test/` 下 pytest 用例，已在 **py311 环境**（`D:\workapp\Anaconda\anaconda3\envs\py311`，现已克隆为 SciMKG 环境）补装 `pytest`、`pytest-asyncio`、`aiosqlite`、`email-validator`。
  - 2026-09-07 修正：此前 `pytest-asyncio`、`aiosqlite` 仅装在默认 Python(3.14)，导致 py311 下 `test_auth_service`/`test_document_service` 报 `fixture 'db' not found` 与 `Unknown pytest.mark.asyncio`，共 7 个错误；补齐后全量测试 **44 passed**。
  - 2026-09-07 Pydantic V2 迁移：全部 `class Config`（settings/schemas）改为 `model_config`（`SettingsConfigDict` / `ConfigDict(from_attributes=True)`），消除 `PydanticDeprecatedSince20` 弃用警告。
  - 2026-09-08 SciMKG 环境：项目统一使用隔离环境 **SciMKG**（Python 3.11.15，`D:\workapp\Anaconda\anaconda3\envs\SciMKG`），由 py311 克隆而来（`conda create -n SciMKG --clone py311 -y`），不再直接使用 py311。克隆后依赖完整（torch/transformers/sentence-transformers/pytest-asyncio/aiosqlite 等就绪），全量测试 **44 passed**。

### 快速安装命令

```powershell
# 1. 安装 Docker Desktop
winget install Docker.DockerDesktop

# 2. 安装完成后重启，然后启动所有服务
# docker-compose -f docker-compose.dev.yml up -d
```

---

## 一、开发环境要求

### 1.1 操作系统

| 系统 | 版本 | 说明 |
|------|------|------|
| Windows | 10/11 | 推荐 WSL2 |
| macOS | 12+ (Monterey) | Intel/Apple Silicon |
| Linux | Ubuntu 22.04+ / CentOS 8+ | 推荐 Ubuntu |

### 1.2 硬件要求

#### 开发环境

| 组件 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | 4 核 | 8 核+ |
| 内存 | 16 GB | 32 GB+ |
| 硬盘 | 100 GB SSD | 500 GB SSD |
| GPU | 无 | NVIDIA RTX 3060+ (8GB VRAM) |

#### 生产环境

| 组件 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | 8 核 | 16 核+ |
| 内存 | 32 GB | 64 GB+ |
| 硬盘 | 500 GB SSD | 2 TB NVMe SSD |
| GPU | 无 | NVIDIA A100/A6000 (40GB+ VRAM) |

---

## 二、基础开发工具

### 2.1 必需工具

| 工具 | 版本 | 用途 | 安装方式 | 本机状态 |
|------|------|------|----------|----------|
| ~~**Git**~~ | ~~2.40+~~ | ~~版本控制~~ | ~~系统包管理器~~ | ✅ 已安装 (2.53.0) |
| ~~**Python**~~ | ~~3.11.x~~ | ~~后端开发~~ | ~~pyenv / 官网下载~~ | ✅ 已安装 (3.14.3) |
| ~~**Node.js**~~ | ~~20.x LTS~~ | ~~前端开发~~ | ~~nvm / 官网下载~~ | ✅ 已安装 (v24.14.0) |
| ~~**pnpm**~~ | ~~8.x+~~ | ~~前端包管理~~ | ~~`npm i -g pnpm`~~ | ✅ 已安装 (10.33.0) |
| **Docker** | 24.x+ | 容器化 | 官网下载 | ❌ 未安装 |
| **Docker Compose** | 2.x+ | 服务编排 | Docker 内置 | ❌ 未安装 |

### 2.2 推荐工具

| 工具 | 用途 | 安装方式 |
|------|------|----------|
| VS Code | 代码编辑器 | 官网下载 |
| Postman / Insomnia | API 测试 | 官网下载 |
| DBeaver / pgAdmin | 数据库管理 | 官网下载 |
| Neo4j Desktop | 图数据库管理 | 官网下载 |
| Redis Insight | Redis 管理 | 官网下载 |
| MinIO Console | 对象存储管理 | Web 界面 |

### 2.3 VS Code 推荐插件

```
前端:
  - ESLint
  - Prettier
  - Tailwind CSS IntelliSense
  - TypeScript and JavaScript Language Features

后端:
  - Python
  - Pylance
  - Ruff
  - Docker

通用:
  - GitLens
  - REST Client
  - Markdown All in One
  - Error Lens
```

---

## 三、后端环境

### 3.1 Python 虚拟环境

<!-- 本机已安装 Python 3.14.3，版本高于推荐的 3.11.x，可直接使用 -->

> **项目实际运行/测试环境为 SciMKG（Python 3.11.15）**：由 py311 环境克隆而来，位于
> `D:\workapp\Anaconda\anaconda3\envs\SciMKG`（解释器 `D:\workapp\Anaconda\anaconda3\envs\SciMKG\python.exe`）。
>
> ```powershell
> conda activate SciMKG
> # 或直接调用解释器
> D:\workapp\Anaconda\anaconda3\envs\SciMKG\python.exe
> ```

~~```bash~~
~~# 创建虚拟环境~~
~~python -m venv venv~~

~~# 激活 (Windows)~~
~~venv\Scripts\activate~~

~~# 激活 (macOS/Linux)~~
~~source venv/bin/activate~~
~~```~~

<!-- 注意: 本机 Python 版本为 3.14.3，位于 C:\Users\wly\AppData\Local\Python\bin\python.exe -->

### 3.2 Python 依赖包

#### 核心依赖 (requirements.txt)

```
# Web 框架
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
starlette==0.35.1

# 数据库
sqlalchemy[asyncio]==2.0.25
aiomysql==0.2.0
alembic==1.13.1
pymysql==1.1.0

# Redis
redis==5.0.1
hiredis==2.3.2

# Neo4j
neo4j==5.16.0

# 认证与安全
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
cryptography==41.0.7

# Celery 任务队列
celery==5.3.6
flower==2.0.1
kombu==5.3.4

# 文件存储
minio==7.2.3
boto3==1.34.14

# PDF 处理
PyMuPDF==1.23.8
pdfplumber==0.10.3
Pillow==10.2.0

# OCR（PaddleOCR，中文优先，扫描件/图片文字页；缺失时 ocr_service 自动降级跳过）
paddlepaddle>=2.6.0
paddleocr>=2.7.0

# 版面分析（LayoutLMv3，复用下方 transformers/Pillow；模型首次运行自动下载，无新增 pip 依赖）

# 公式识别（LaTeX-OCR / pix2tex，公式图 → LaTeX；缺失时 formula_service 自动降级为空）
pix2tex>=0.1.2
munch>=2.5.0

# 图表/公式检测（YOLOv8/ultralytics；缺失时 figure_detection_service 自动降级）
ultralytics>=8.0.0

# 图表描述（BLIP-2，复用 transformers/torch；缺失时 chart_description_service 自动降级）

# AI/ML 框架
transformers==4.37.0
torch==2.1.2
sentence-transformers==2.2.2
faiss-cpu==1.7.4
accelerate==0.26.1
peft==0.7.1
bitsandbytes==0.42.0

# HTTP 客户端
httpx==0.26.0
aiohttp==3.9.1
requests==2.31.0

# 数据验证
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# 日志
loguru==0.7.2
python-json-logger==2.0.7

# 工具
python-dotenv==1.0.0
tenacity==8.2.3
pyyaml==6.0.1
```

#### 开发依赖 (requirements-dev.txt)

```
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
pytest-mock==3.12.0
httpx==0.26.0
ruff==0.1.9
black==23.12.1
mypy==1.8.0
pre-commit==3.6.0
factory-boy==3.3.0
faker==21.0.0
```

#### GPU 依赖 (requirements-gpu.txt) - 可选

```
torch==2.1.2+cu121
faiss-gpu==1.7.4
vllm==0.2.7
```

### 3.3 环境变量配置 (.env)

```env
# 应用配置
APP_NAME=Scientific Document Parser
APP_VERSION=1.0.0
DEBUG=true
API_PREFIX=/api/v1

# 数据库配置
DATABASE_URL=mysql+aiomysql://root:20020522@localhost:3306/WX
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=10

# Redis 配置
REDIS_URL=redis://localhost:6379/0

# Neo4j 配置
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=wang245484@

# MinIO 配置
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=documents
MINIO_SECURE=false

# JWT 配置
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Celery 配置
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# AI 模型配置
EMBEDDING_MODEL_PATH=moulds/bge-m3
LLM_MODEL_PATH=moulds/qwen-7b
LLM_MAX_TOKENS=4096
LLM_TEMPERATURE=0.7
LLM_DEVICE=cuda

# OCR 配置（PaddleOCR，中文优先，扫描件/图片文字页）
OCR_ENABLED=true
OCR_LANGUAGE=ch
OCR_USE_ANGLE_CLS=true
OCR_DPI=200
OCR_MODEL_PATH=moulds/paddleocr

# 版面分析配置（LayoutLMv3，PubLayNet 微调）
LAYOUT_ENABLED=true
LAYOUT_MODEL=HYPJUDY/layoutlmv3-base-finetuned-publaynet
LAYOUT_MODEL_PATH=moulds/layoutlmv3
LAYOUT_DEVICE=cpu

# 公式识别配置（LaTeX-OCR / pix2tex，公式图 → LaTeX）
FORMULA_ENABLED=true
FORMULA_MODEL=lukas-blecher/LaTeX-OCR
FORMULA_MODEL_PATH=moulds/latex-ocr
FORMULA_DEVICE=cpu

# 图表/公式检测配置（YOLOv8）
FIGURE_DETECT_ENABLED=true
FIGURE_DETECT_MODEL_PATH=moulds/yolov8
FIGURE_DETECT_CONF=0.25

# 图表描述配置（BLIP-2）
CHART_CAPTION_ENABLED=true
CHART_CAPTION_MODEL=Salesforce/blip2-opt-2.7b
CHART_CAPTION_MODEL_PATH=moulds/blip2
CHART_CAPTION_DEVICE=cpu

# NER 配置（SciBERT 微调 token classification，需微调后含 id2label 的 checkpoint）
NER_ENABLED=true
NER_MODEL=allenai/scibert_scivocab_uncased
NER_MODEL_PATH=moulds/scibert
NER_DEVICE=cpu
NER_AGGREGATION=max

# 实体链接配置（Wikidata/DBpedia 公共知识图谱；可插拔可降级）
ENTITY_LINKING_ENABLED=true
ENTITY_LINKING_BACKEND=wikidata
ENTITY_LINKING_LANGUAGE=zh
ENTITY_LINKING_TIMEOUT=5.0

# 关系推理 / 图谱补全配置（规则推理 + TransE 链路预测；可插拔可降级）
RELATION_INFERENCE_ENABLED=true
RELATION_INFERENCE_BACKEND=transe
TRANSE_DIM=50
TRANSE_EPOCHS=300
TRANSE_LR=0.01
TRANSE_MARGIN=1.0
TRANSE_SEED=42

# 语义搜索配置（分面搜索 + 搜索建议；纯 Python 实现，零依赖）
SEMANTIC_SEARCH_ENABLED=true

# Text-to-Cypher 配置（自然语言 → Cypher；规则模板，LLM 后端预留可降级）
TEXT_TO_CYPHER_ENABLED=true
TEXT_TO_CYPHER_BACKEND=rule

# 主动学习采样配置（不确定性 / 多样性 / QBC / 混合；纯 Python 零依赖可降级）
ACTIVE_LEARNING_ENABLED=true
ACTIVE_LEARNING_STRATEGY=hybrid
ACTIVE_LEARNING_UNCERTAINTY=entropy
ACTIVE_LEARNING_DIVERSITY=core_set
ACTIVE_LEARNING_QBC=vote_entropy
ACTIVE_LEARNING_SEED=42

# 标注一致性评估配置（Fleiss' Kappa / Cohen's Kappa / Krippendorff's Alpha；纯 Python 零依赖）
ANNOTATION_AGREEMENT_ENABLED=true

# 数据/模型版本管理配置（DVC 数据版本 + MLflow 实验追踪/模型注册 + 自动重训管道；纯 Python 零依赖可降级）
VERSION_MGMT_ENABLED=true
VERSION_MGMT_BACKEND=builtin
VERSION_MGMT_STORE_PATH=
RETRAIN_THRESHOLD=100
RETRAIN_PERIODIC_DAYS=7
RETRAIN_MAX_RETRIES=3

# 置信度校准与漂移检测配置（Temperature/Platt Scaling、数据漂移告警；纯 Python 零依赖）
CALIBRATION_DRIFT_ENABLED=true
CALIBRATION_ECE_BINS=10
DRIFT_PSI_WARNING=0.1
DRIFT_PSI_ALERT=0.25

# 文件上传配置
MAX_UPLOAD_SIZE=104857600
ALLOWED_EXTENSIONS=pdf

# 日志配置
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
```

---

## 四、前端环境

### 4.1 Node.js 配置

<!-- 本机已安装 Node.js v24.14.0，版本高于推荐的 20.x，可直接使用 -->

~~```bash~~
~~# 使用 nvm 管理 Node.js 版本~~
~~nvm install 20~~
~~nvm use 20~~

~~# 安装 pnpm~~
~~npm install -g pnpm~~

~~# 验证安装~~
~~node -v   # v20.x.x~~
~~# 本机实际: v24.14.0~~
~~pnpm -v   # 8.x.x~~
~~```~~

<!-- 注意: pnpm 尚未安装，需运行: npm install -g pnpm -->

### 4.2 前端依赖包

#### 生产依赖 (package.json)

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.21.0",
    "antd": "^5.12.0",
    "@ant-design/icons": "^5.2.6",
    "zustand": "^4.4.7",
    "@tanstack/react-query": "^5.17.0",
    "@tanstack/react-query-devtools": "^5.17.0",
    "axios": "^1.6.5",
    "pdfjs-dist": "^3.11.174",
    "react-pdf": "^7.7.0",
    "echarts": "^5.4.3",
    "echarts-for-react": "^3.0.2",
    "react-force-graph-2d": "^1.25.5",
    "react-force-graph-3d": "^1.24.1",
    "katex": "^0.16.9",
    "rehype-katex": "^7.0.0",
    "remark-math": "^6.0.0",
    "react-markdown": "^9.0.1",
    "dayjs": "^1.11.10",
    "lodash-es": "^4.17.21",
    "clsx": "^2.1.0",
    "immer": "^10.0.3"
  }
}
```

#### 开发依赖

```json
{
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@types/lodash-es": "^4.17.12",
    "@types/katex": "^0.16.7",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "@vitejs/plugin-react": "^4.2.1",
    "eslint": "^8.55.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.5",
    "prettier": "^3.1.1",
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.32",
    "autoprefixer": "^10.4.16"
  }
}
```

### 4.3 环境变量配置

#### .env.development

```env
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_WS_URL=ws://localhost:8000/ws
VITE_APP_TITLE=科学文献解析平台 (开发)
```

#### .env.production

```env
VITE_API_BASE_URL=/api/v1
VITE_WS_URL=/ws
VITE_APP_TITLE=科学文献解析平台
```

---

## 五、数据库环境

### 5.1 MySQL

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 版本 | 8.0.x | 推荐最新稳定版 |
| 端口 | 3306 | 默认端口 |
| 数据库名 | WX | 主数据库 |
| 用户名 | root | 管理员 |
| 密码 | 20020522 | 生产环境需修改 |
| 字符集 | utf8mb4 | 支持完整 Unicode |

**Docker 启动**

```bash
docker run -d \
  --name mysql \
  -e MYSQL_ROOT_PASSWORD=20020522 \
  -e MYSQL_DATABASE=WX \
  -p 3306:3306 \
  -v mysql_data:/var/lib/mysql \
  mysql:8.0 --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
```

**本地安装**

```bash
# Ubuntu
sudo apt install mysql-server-8.0

# macOS
brew install mysql

# Windows
# 下载安装包: https://dev.mysql.com/downloads/mysql/

# 初始化数据库
mysql -u root -p
CREATE DATABASE WX CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 5.2 Redis

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 版本 | 7.x | 推荐最新稳定版 |
| 端口 | 6379 | 默认端口 |
| 数据库 | 0-3 | 0:缓存, 1:Celery broker, 2:Celery backend |
| 密码 | - | 开发环境可不设 |

**Docker 启动**

```bash
docker run -d \
  --name redis \
  -p 6379:6379 \
  redis:7 redis-server --appendonly yes
```

### 5.3 Neo4j

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 版本 | 5.x | 推荐最新稳定版 |
| Bolt 端口 | 7687 | 驱动连接 |
| HTTP 端口 | 7474 | Web 界面 |
| 用户名 | neo4j | 默认 |
| 密码 | password | 首次启动需修改 |
| 插件 | APOC | 必需安装 |

**Docker 启动**

```bash
docker run -d \
  --name neo4j \
  -e NEO4J_AUTH=neo4j/wang245484@ \
  -e NEO4J_PLUGINS='["apoc"]' \
  -p 7474:7474 \
  -p 7687:7687 \
  -v neo4j_data:/data \
  -v neo4j_logs:/logs \
  neo4j:5
```

---

## 六、对象存储环境

### 6.1 MinIO (开发环境)

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 版本 | latest | 推荐最新 |
| API 端口 | 9000 | S3 兼容 API |
| Console 端口 | 9001 | Web 管理界面 |
| Access Key | minioadmin | 默认 |
| Secret Key | minioadmin | 默认 |
| Bucket | documents | 文档存储 |

**Docker 启动**

```bash
docker run -d \
  --name minio \
  -p 9000:9000 \
  -p 9001:9001 \
  -e MINIO_ROOT_USER=minioadmin \
  -e MINIO_ROOT_PASSWORD=minioadmin \
  -v minio_data:/data \
  minio/minio server /data --console-address ":9001"
```

### 6.2 云对象存储 (生产环境)

| 服务 | 说明 |
|------|------|
| 阿里云 OSS | 国内推荐，低延迟 |
| AWS S3 | 国际推荐，高可用 |
| 腾讯云 COS | 备选方案 |

---

## 七、AI 模型环境

### 7.1 模型清单

> 模型统一存放在项目根目录 `D:\work\VSCodeWork\LX\moulds`，每个模型一个独立子文件夹；
> 代码侧通过 `resolve_model_source(hf_name, local_dir)` 优先加载本地目录，缺失时回退 HuggingFace 名称自动下载。

| 模型 | 用途 | 大小 | 存储位置 |
|------|------|------|----------|
| PaddleOCR | OCR（中文优先，扫描件/图片页） | ~15 MB（检测+识别模型） | `moulds/paddleocr`（缺失时首次运行自动下载到 `~/.paddleocr`） |
| BGE-M3 | 文本向量化 | ~2.2 GB | `moulds/bge-m3` |
| Qwen 2.5-7B | LLM 推理 | ~15 GB | `moulds/qwen-7b` |
| LayoutLMv3 | 版面分析（PubLayNet 微调） | ~360 MB | `moulds/layoutlmv3`（缺失回退 `HYPJUDY/layoutlmv3-base-finetuned-publaynet` 自动下载） |
| YOLOv8 | 图表/公式检测 | ~20 MB | `moulds/yolov8`（放 `*.pt`；缺失时 `figure_detection_service` 自动降级） |
| LaTeX-OCR | 公式识别 | ~300 MB | `moulds/latex-ocr`（放 `weights.pth`，缺失时 pix2tex 自动下载） |
| BLIP-2 | 图表描述 | ~4 GB | `moulds/blip2`（缺失回退 `Salesforce/blip2-opt-2.7b` 自动下载） |
| SciBERT | NER（token classification） | ~450 MB | `moulds/scibert`（需微调后含 `id2label` 的 checkpoint） |

### 7.2 模型下载

```bash
# 使用 HuggingFace CLI 下载（放置到 moulds 对应子文件夹）
pip install huggingface-hub

# BGE-M3 嵌入模型
huggingface-cli download BAAI/bge-m3 --local-dir moulds/bge-m3

# Qwen 2.5-7B
huggingface-cli download Qwen/Qwen2.5-7B --local-dir moulds/qwen-7b

# LayoutLMv3（版面分析，PubLayNet 微调；layout_service 缺失时自动下载）
huggingface-cli download HYPJUDY/layoutlmv3-base-finetuned-publaynet --local-dir moulds/layoutlmv3

# LaTeX-OCR（pix2tex 权重：将 weights.pth 放入 moulds/latex-ocr；无本地权重时自动下载）

# SciBERT NER（需使用「微调后含 id2label」的 token classification checkpoint；
# 基座 scibert_scivocab_uncased 为 MLM、不可直接做 NER。可将微调后的模型目录放入 moulds/scibert）
# huggingface-cli download <微调后的ner模型> --local-dir moulds/scibert
```

### 7.3 GPU 环境配置

```bash
# 检查 NVIDIA 驱动
nvidia-smi

# 安装 CUDA Toolkit (根据 GPU 型号选择版本)
# 推荐 CUDA 12.1+

# 安装 cuDNN
# 下载: https://developer.nvidia.com/cudnn

# 验证 PyTorch GPU 支持
python -c "import torch; print(torch.cuda.is_available())"
```

### 7.4 模型服务配置

```bash
# 启动嵌入服务
python -m ai_services.embedding_service \
  --model moulds/bge-m3 \
  --port 8001 \
  --device cuda

# 启动 LLM 服务 (使用 vLLM)
python -m vllm.entrypoints.api_server \
  --model moulds/qwen-7b \
  --port 8002 \
  --tensor-parallel-size 1 \
  --max-model-len 4096
```

---

## 八、GraphRAGTest 子系统环境

> GraphRAGTest 是基于 Neo4j 知识图谱 + Qwen LLM 的本地 GraphRAG 问答子系统

### 8.1 子系统概述

| 项目 | 配置 |
|------|------|
| 端口 | 8001 |
| 默认模型 | qwen/qwen3-4b-2507 |
| LLM 接入方式 | LM Studio (localhost:1234) |
| 依赖服务 | Neo4j (7687) |

### 8.2 启动方式

```bash
# 1. 确保 LM Studio 已运行并加载模型 (qwen/qwen3-4b-2507)
#    LM Studio 需开启 "Server" 功能，默认端口 1234

# 2. 启动 GraphRAGTest
cd GraphRAGTest
pip install -r requirements.txt
python app.py

# 3. 访问 API 文档
# http://localhost:8001/docs
```

### 8.3 依赖文件

```bash
# GraphRAGTest/requirements.txt
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
httpx>=0.27.0
neo4j>=5.16.0
sentence-transformers>=2.2.0   # BGE-M3 嵌入（可选，缺失时 vector_store 降级为哈希向量）
faiss-cpu>=1.7.4                # 向量检索加速（可选，缺失时用 numpy 暴力检索）
tenacity>=8.2.0
loguru>=0.7.0
```

> 向量检索模块 `vector_store.py`（2026-09-07 新增）：基于 FAISS + BGE-M3 的实体/关系/文档 chunk 向量写入与相似度检索。
> `sentence-transformers` 与 `faiss` 均为可选依赖，缺失时自动降级为「哈希向量 + numpy 暴力检索」，接口不受影响。
>
> 混合检索模块 `hybrid_retrieval.py`（2026-09-07 新增）：向量相似度 + BM25 关键词打分，用 RRF（Reciprocal Rank Fusion）融合；可选集成 `BAAI/bge-reranker-base`（Cross-Encoder）对候选精排，未安装 `sentence-transformers` 或模型缺失时自动跳过（`rerank_backend=none`）。
> 重排序模型由 `config.py` 的 `GRAPHRAG_RERANK_MODEL` / `GRAPHRAG_RERANK_DEVICE` 控制。
>
> **已真实跑通（2026-09-07）**：安装 `torch==2.14.0+cpu` + `sentence-transformers==6.0.1`，并下载
> `BAAI/bge-m3`（约 2.2GB）与 `BAAI/bge-reranker-base`（约 1.1GB）到 `~/.cache/huggingface/hub`。
> 验证脚本 `test/verify_sentence_transformers.py` 确认：嵌入 backend=`sentence-transformers-bge-m3`（dim=1024），
> 精排 backend=`cross-encoder`，向量检索 + 混合检索 + 精排结果正确。
>
> 两个环境细节已由 `config.py` 在导入时自动处理，无需手动设置：
> - `KMP_DUPLICATE_LIB_OK=TRUE` — 规避 Anaconda(MKL) 与 torch 的 libiomp5md.dll 冲突（OMP Error #15）。
> - `HF_ENDPOINT=https://hf-mirror.com` — 国内镜像下载模型（已被真实环境变量优先）。
>
> 索引管线（2026-09-07 打通）：`hybrid_retrieval.py` 新增 `index_document_vectors(doc_id, entities, relations, chunks)`，
> 将文档实体/关系/chunk 统一写入混合检索器（向量库 + BM25）；`indexer.py` 的 `index_document()` 新增 `chunks` 参数，
> 将 chunk 以 `Chunk` 节点入 Neo4j（`Document-[:CONTAINS]->Chunk`）并同步向量化；新增 `POST /api/v1/hybrid/search` 混合检索接口。
>
> 模型路径（2026-09-07 统一到 moulds）：`config.py` 新增 `GRAPHRAG_EMBEDDING_MODEL_PATH`（`moulds/bge-m3`）、
> `GRAPHRAG_RERANK_MODEL_PATH`（`moulds/bge-reranker-base`），并提供 `resolve_model_source(hf_name, local_dir)`：
> 本地目录存在且非空时优先加载，否则回退 HuggingFace 名称自动下载。

### 8.4 环境变量 (.env)

```env
# GraphRAGTest/.env
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=wang245484@

LM_STUDIO_BASE_URL=http://localhost:1234/v1
DEFAULT_MODEL=qwen/qwen3-4b-2507

API_HOST=0.0.0.0
API_PORT=8001
API_TITLE=GraphRAG API
API_VERSION=1.0.0
```

### 8.5 核心接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/health` | GET | 服务健康状态 |
| `/api/v1/query` | POST | GraphRAG 问答 |
| `/api/v1/graph/stats` | GET | 图谱统计 |
| `/api/v1/graph/nodes/{label}` | GET | 按标签查询节点 |
| `/api/v1/graph/nodes/keyword/{keyword}` | GET | 关键词搜索节点 |
| `/api/v1/index/document` | POST | 文档索引 |
| `/api/v1/hybrid/search` | POST | 混合检索（向量 + BM25 + RRF） |
| `/api/v1/llm/status` | GET | LLM 状态 |
| `/api/v1/llm/load` | POST | 加载模型 |

---

## 九、Docker Compose 完整配置

### 9.1 开发环境 (docker-compose.dev.yml)

```yaml
version: "3.8"

services:
  mysql:
    image: mysql:8.0
    container_name: sdp-mysql
    environment:
      MYSQL_ROOT_PASSWORD: 20020522
      MYSQL_DATABASE: WX
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7
    container_name: sdp-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  neo4j:
    image: neo4j:5
    container_name: sdp-neo4j
    environment:
      NEO4J_AUTH: neo4j/password
      NEO4J_PLUGINS: '["apoc"]'
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:7474 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio
    container_name: sdp-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD-SHELL", "mc ready local || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sdp-backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: mysql+aiomysql://root:20020522@mysql:3306/WX
      REDIS_URL: redis://redis:6379/0
      NEO4J_URI: bolt://neo4j:7687
      NEO4J_USER: neo4j
      NEO4J_PASSWORD: wang245484@
      MINIO_ENDPOINT: minio:9000
      CELERY_BROKER_URL: redis://redis:6379/1
      CELERY_RESULT_BACKEND: redis://redis:6379/2
    volumes:
      - ./backend:/app
      - ./moulds:/app/moulds
      depends_on:
        mysql:
          condition: service_healthy
        redis:
          condition: service_healthy
        neo4j:
          condition: service_healthy
        minio:
          condition: service_healthy

  celery-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sdp-celery
    command: celery -A app.core.celery_app worker -l info -Q parsing,extraction,graph
    environment:
      DATABASE_URL: mysql+aiomysql://root:20020522@mysql:3306/WX
      REDIS_URL: redis://redis:6379/0
      NEO4J_URI: bolt://neo4j:7687
      CELERY_BROKER_URL: redis://redis:6379/1
      CELERY_RESULT_BACKEND: redis://redis:6379/2
    volumes:
      - ./backend:/app
      - ./moulds:/app/moulds
    depends_on:
      - redis
      - mysql
      - neo4j
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: sdp-frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      VITE_API_BASE_URL: http://localhost:8000/api/v1
    depends_on:
      - backend

  flower:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sdp-flower
    command: celery -A app.core.celery_app flower --port=5555
    ports:
      - "5555:5555"
    environment:
      CELERY_BROKER_URL: redis://redis:6379/1
    depends_on:
      - redis

volumes:
  mysql_data:
  redis_data:
  neo4j_data:
  neo4j_logs:
  minio_data:
```

### 9.2 启动所有服务

```bash
# 启动所有基础服务
docker-compose -f docker-compose.dev.yml up -d mysql redis neo4j minio

# 等待服务就绪
sleep 15

# 启动完整应用
docker-compose -f docker-compose.dev.yml up -d

# 查看服务状态
docker-compose -f docker-compose.dev.yml ps

# 查看日志
docker-compose -f docker-compose.dev.yml logs -f

# 停止所有服务
docker-compose -f docker-compose.dev.yml down

# 停止并删除数据卷
docker-compose -f docker-compose.dev.yml down -v
```

---

## 十、服务端口汇总

| 服务 | 端口 | 协议 | 说明 | 本机状态 |
|------|------|------|------|----------|
| 前端开发服务器 | 3000 | HTTP | Vite Dev Server | ❌ 未部署 |
| 后端 API | 8000 | HTTP | FastAPI + Uvicorn | ❌ 未部署 |
| GraphRAGTest API | 8001 | HTTP | GraphRAG 问答子系统 | ❌ 未部署 |
| MySQL | 3306 | TCP | 关系型数据库 | ✅ 已部署 |
| Redis | 6379 | TCP | 缓存/消息队列 | ✅ 已部署 |
| Neo4j (HTTP) | 7474 | HTTP | Web 管理界面 | ✅ 已部署 |
| Neo4j (Bolt) | 7687 | TCP | 驱动连接 | ✅ 已部署 |
| MinIO (API) | 9000 | HTTP/S3 | 对象存储 API | ✅ 已部署 |
| MinIO (Console) | 9001 | HTTP | Web 管理界面 | ✅ 已部署 |
| Flower | 5555 | HTTP | Celery 监控 | ❌ 未部署 |
| LM Studio | 1234 | HTTP | LLM API 服务 | ❌ 未部署 |
| Nginx (生产) | 80/443 | HTTP/HTTPS | 反向代理 | ❌ 未部署 |

---

## 十一、网络配置

### 11.1 开发环境网络

```
localhost:3000  →  前端 (React)
localhost:8000  →  后端 API (FastAPI)
localhost:3306  →  MySQL
localhost:6379  →  Redis
localhost:7474  →  Neo4j Browser
localhost:7687  →  Neo4j Bolt
localhost:9000  →  MinIO API
localhost:9001  →  MinIO Console
localhost:5555  →  Flower 监控
```

### 11.2 生产环境网络

```
Nginx (80/443)
  ├── /        →  前端静态文件
  ├── /api/*   →  后端服务 (8000)
  └── /ws/*    →  WebSocket (8000)

内部网络 (Docker Network)
  ├── backend:8000
  ├── mysql:3306
  ├── redis:6379
  ├── neo4j:7687
  ├── minio:9000
  └── celery-worker
```

---

## 十二、一键启动脚本

### 12.1 开发环境启动 (start-dev.sh)

```bash
#!/bin/bash
echo "=== 启动开发环境 ==="

# 检查 Docker
if ! command -v docker &> /dev/null; then
    echo "错误: Docker 未安装"
    exit 1
fi

# 检查 Docker Compose
if ! command -v docker-compose &> /dev/null; then
    echo "错误: Docker Compose 未安装"
    exit 1
fi

# 启动基础服务
echo "启动数据库和缓存服务..."
docker-compose -f docker-compose.dev.yml up -d mysql redis neo4j minio

# 等待服务就绪
echo "等待服务启动..."
sleep 15

# 初始化数据库
echo "初始化数据库..."
cd backend
source venv/bin/activate 2>/dev/null || venv\Scripts\activate 2>/dev/null
alembic upgrade head
cd ..

# 创建 MinIO Bucket
echo "创建 MinIO Bucket..."
docker exec sdp-minio mc mb --ignore-existing data/documents

# 启动应用
echo "启动应用服务..."
docker-compose -f docker-compose.dev.yml up -d

echo "=== 开发环境启动完成 ==="
echo "前端: http://localhost:3000"
echo "后端 API: http://localhost:8000/docs"
echo "Neo4j: http://localhost:7474"
echo "MinIO: http://localhost:9001"
echo "Flower: http://localhost:5555"
```

### 12.2 停止脚本 (stop-dev.sh)

```bash
#!/bin/bash
echo "=== 停止开发环境 ==="
docker-compose -f docker-compose.dev.yml down
echo "=== 已停止所有服务 ==="
```

---

## 十三、常见问题排查

### 13.1 MySQL 连接失败

```bash
# 检查服务状态
docker ps | grep mysql

# 查看日志
docker logs sdp-mysql

# 测试连接
mysql -h localhost -u root -p20020522 -D WX
```

### 13.2 Redis 连接失败

```bash
# 检查服务状态
docker ps | grep redis

# 测试连接
docker exec -it sdp-redis redis-cli ping
```

### 13.3 Neo4j 启动慢

```bash
# 查看日志
docker logs -f sdp-neo4j

# 增加内存限制
docker run --memory=4g ...
```

### 13.4 GPU 不可用

```bash
# 检查 NVIDIA 驱动
nvidia-smi

# 检查 Docker GPU 支持
docker run --gpus all nvidia/cuda:12.1.0-base-ubuntu22.04 nvidia-smi

# 安装 NVIDIA Container Toolkit
# https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/install-guide.html
```

### 13.5 端口冲突

```bash
# 查看端口占用 (Linux/macOS)
lsof -i :8000
netstat -tulpn | grep 8000

# 查看端口占用 (Windows)
netstat -ano | findstr :8000

# 修改 docker-compose.yml 中的端口映射
```

---

## 十四、GraphRAGTest 子系统配置

### 14.1 概述

**GraphRAGTest** 是一个独立的本地 GraphRAG 问答系统，基于 Neo4j 知识图谱 + Qwen LLM（通过 LM Studio 本地部署）。

### 14.2 目录结构

```
GraphRAGTest/
├── app.py                  # FastAPI 应用入口 (端口 8001)
├── config.py               # 配置文件
├── .env                    # 环境变量
├── requirements.txt        # Python 依赖
├── neo4j_client.py         # Neo4j 客户端
├── lmstudio_client.py      # LM Studio 客户端
├── graphrag_service.py     # GraphRAG 核心服务
├── indexer.py             # 文档索引器
└── api/
    └── routes.py           # API 路由
```

### 14.3 环境配置 (.env)

```env
# LM Studio
LMSTUDIO_BASE_URL=http://localhost:1234
LMSTUDIO_MODEL=qwen/qwen3-4b-2507

# Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=wang245484@
NEO4J_DATABASE=neo4j

# API Server
API_HOST=0.0.0.0
API_PORT=8001
```

### 14.4 依赖安装

```bash
cd GraphRAGTest
pip install -r requirements.txt
```

**核心依赖**:
- fastapi>=0.115.0
- uvicorn[standard]>=0.30.0
- pydantic>=2.5.0
- neo4j>=5.16.0
- httpx>=0.27.0
- loguru>=0.7.0

### 14.5 前置条件

1. **LM Studio** 已运行并加载模型 (默认端口 1234)
   - 下载地址: https://lmstudio.ai/
   - 加载模型: qwen/qwen3-4b-2507 或其他 Qwen 模型

2. **Neo4j** 已启动 (默认端口 7687)
   - 本地安装或 Docker 启动
   - 确保认证密码与 .env 中配置一致

### 14.6 启动方式

```bash
cd GraphRAGTest

# 方式1: 直接运行
python app.py

# 方式2: 使用 uvicorn
uvicorn app:app --host 0.0.0.0 --port 8001 --reload

# 方式3: Windows 批处理
start.bat
```

### 14.7 服务地址

| 服务 | 地址 | 说明 |
|------|------|------|
| API 服务 | http://localhost:8001 | FastAPI 应用 |
| Swagger UI | http://localhost:8001/docs | API 文档 |
| ReDoc | http://localhost:8001/redoc | 备用文档 |

### 14.8 API 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/health` | GET | 健康检查 |
| `/api/v1/graph/stats` | GET | 图谱统计 |
| `/api/v1/graph/nodes/{label}` | GET | 按标签查询节点 |
| `/api/v1/graph/nodes/keyword/{keyword}` | GET | 关键词搜索 |
| `/api/v1/graph/relations/type/{rel_type}` | GET | 按类型查询关系 |
| `/api/v1/graph/nodes` | POST | 添加节点 |
| `/api/v1/graph/relations` | POST | 添加关系 |
| `/api/v1/graph/batch` | POST | 批量导入 |
| `/api/v1/graph/clear` | DELETE | 清空图谱 |
| `/api/v1/query` | POST | GraphRAG 问答 |
| `/api/v1/query/history` | GET | 查询历史 |
| `/api/v1/llm/models` | GET | 列出模型 |
| `/api/v1/llm/status` | GET | LLM 状态 |
| `/api/v1/llm/load` | POST | 加载模型 |
| `/api/v1/llm/unload` | POST | 卸载模型 |
| `/api/v1/llm/generate` | POST | 直接生成 |
| `/api/v1/index/document` | POST | 文档索引 |
| `/api/v1/hybrid/search` | POST | 混合检索（向量 + BM25 + RRF） |

### 14.9 常见问题

#### Neo4j 认证失败

```bash
# 检查 Neo4j 状态
neo4j status

# 重置密码（首次安装或忘记密码）
# 1. 打开 http://localhost:7474
# 2. 使用当前密码登录
# 3. 设置新密码
# 4. 更新 .env 中的 NEO4J_PASSWORD
```

#### LM Studio 连接失败

```bash
# 检查 LM Studio 是否运行
# 确保 LM Studio 已启动并开启 Local Server (默认端口 1234)
# Settings → Local Server → 勾选 Enable Local Server
```

#### 导入路径错误

如果遇到 `ImportError: cannot import name 'xxx'` 错误，确保从项目根目录运行：

```bash
cd D:\work\VSCodeWork\aiTest1\GraphRAGTest
python app.py
```

### 14.10 与主后端集成

主后端 (8000) 通过 `/api/v1/knowledge-graph/rag/*` 路由代理到 GraphRAGTest (8001)：

| 主后端路由 | 代理目标 |
|-----------|---------|
| `GET /api/v1/knowledge-graph/rag/health` | GraphRAGTest `/api/v1/health` |
| `GET /api/v1/knowledge-graph/rag/llm/status` | GraphRAGTest `/api/v1/llm/status` |
| `POST /api/v1/knowledge-graph/rag/query` | GraphRAGTest `/api/v1/query` |
| `POST /api/v1/knowledge-graph/rag/index/document` | GraphRAGTest `/api/v1/index/document` |

详细代理配置见 `backend/app/services/graphrag_integration.py`。
