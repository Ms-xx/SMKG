# 敏感信息（占位）。生产环境应改用外部 Secret 管理（如 Vault / SealedSecrets / 云厂商 KMS）。
# 切勿在本文件提交真实凭据。
apiVersion: v1
kind: Secret
metadata:
  name: smkg-secret
  namespace: smkg
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "CHANGE_ME"
  DATABASE_URL: "mysql+aiomysql://root:CHANGE_ME@mysql:3306/wx"
  NEO4J_PASSWORD: "CHANGE_ME"
  MINIO_SECRET_KEY: "minioadmin"
  JWT_SECRET_KEY: "change-me-in-production"